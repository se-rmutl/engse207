# 🐳 คู่มือเตรียมเครื่องสำหรับ Docker
## สำหรับ Windows 11 + WSL2 + VS Code

**วิชา:** ENGSE207 สถาปัตยกรรมซอฟต์แวร์  
**มหาวิทยาลัย:** มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา  
**ผู้สอน:** นายธนิต เกตุแก้ว  
**ปรับปรุงล่าสุด:** มกราคม 2569

---

## 📋 สารบัญ

1. [วัตถุประสงค์](#1-วัตถุประสงค์)
2. [Docker คืออะไร?](#2-docker-คืออะไร)
3. [ความต้องการของระบบ](#3-ความต้องการของระบบ)
4. [ขั้นตอนที่ 1: ตรวจสอบ WSL2](#4-ขั้นตอนที่-1-ตรวจสอบ-wsl2)
5. [ขั้นตอนที่ 2: ติดตั้ง Docker Desktop](#5-ขั้นตอนที่-2-ติดตั้ง-docker-desktop)
6. [ขั้นตอนที่ 3: ตั้งค่า Docker Desktop](#6-ขั้นตอนที่-3-ตั้งค่า-docker-desktop)
7. [ขั้นตอนที่ 4: ตรวจสอบการติดตั้ง](#7-ขั้นตอนที่-4-ตรวจสอบการติดตั้ง)
8. [ขั้นตอนที่ 5: สมัคร Docker Hub](#8-ขั้นตอนที่-5-สมัคร-docker-hub)
9. [แบบฝึกหัด: ทดลองใช้งาน Docker](#9-แบบฝึกหัด-ทดลองใช้งาน-docker)
10. [Checklist ก่อนมาเรียน](#10-checklist-ก่อนมาเรียน)
11. [การแก้ปัญหาเบื้องต้น](#11-การแก้ปัญหาเบื้องต้น)

---

## 1. วัตถุประสงค์

เอกสารนี้จัดทำขึ้นเพื่อให้นักศึกษา:

✅ เข้าใจว่า Docker คืออะไรและทำงานอย่างไร  
✅ ติดตั้ง Docker Desktop บน Windows 11 + WSL2 ได้สำเร็จ  
✅ สมัครบัญชี Docker Hub และ Login ได้  
✅ ทดลองใช้คำสั่ง Docker พื้นฐานได้  
✅ พร้อมสำหรับการเรียน Docker ในชั้นเรียน  

**⏱️ เวลาที่ใช้โดยประมาณ:** 30-45 นาที

---

## 2. Docker คืออะไร?

### 2.1 นิยามอย่างง่าย

**Docker** คือเครื่องมือที่ช่วยให้เรา "แพ็ค" แอปพลิเคชันพร้อมทุกอย่างที่ต้องใช้ (Code, Runtime, Libraries, Settings) ไว้ในกล่องเดียวเรียกว่า **Container** แล้วสามารถนำไปรันได้ทุกที่ที่มี Docker

```
┌─────────────────────────────────────────────────────────────────┐
│                     Docker ทำอะไรได้?                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   📦 BUILD    →   สร้าง Container จาก Application ของเรา         │
│   🚢 SHIP     →   แชร์ Container ผ่าน Docker Hub                  │
│   🏃 RUN      →   รัน Container ได้ทุกเครื่องที่มี Docker               │
│                                                                 │
│   "Build Once, Run Anywhere"                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 ทำไมต้องใช้ Docker?

**ปัญหาแบบเดิม:**
```
👨‍💻 Developer: "Code ทำงานได้ดีบนเครื่องผม!"
🖥️ Server:    "แต่มันไม่ทำงานบน Server!"
😱 ทุกคน:      "ทำไม?!"

สาเหตุ:
- Node.js version ไม่ตรงกัน
- Library version ต่างกัน  
- OS settings ต่างกัน
- Environment ต่างกัน
```

**แก้ปัญหาด้วย Docker:**
```
👨‍💻 Developer: "ผมสร้าง Container แล้ว"
🖥️ Server:    "ผม Pull มารัน... ทำงานได้เลย!"
🎉 ทุกคน:      "เยี่ยม!"

เพราะ:
- ทุกอย่างอยู่ใน Container เดียวกัน
- รันได้เหมือนกันทุกที่
- ไม่มีปัญหา "เครื่องผมทำงานได้" อีกต่อไป
```

### 2.3 คำศัพท์สำคัญ

| คำศัพท์ | ความหมาย | เปรียบเทียบ |
|---------|----------|-------------|
| **Image** | Template สำหรับสร้าง Container (ไม่เปลี่ยนแปลง) | เหมือน Class ใน OOP |
| **Container** | Instance ที่รันจาก Image (รันได้, หยุดได้) | เหมือน Object ใน OOP |
| **Dockerfile** | ไฟล์คำสั่งสำหรับสร้าง Image | เหมือน Recipe |
| **Docker Hub** | ที่เก็บ Images บน Cloud | เหมือน GitHub สำหรับ Code |
| **Registry** | ที่เก็บ Docker Images | เหมือน npm registry |

### 2.4 Docker Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Docker Architecture                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Docker Client (CLI)                   │   │
│  │         docker build | docker pull | docker run          │   │
│  └─────────────────────────┬────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                Docker Desktop (Windows)                  │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │              Docker Engine (WSL2)                  │  │   │
│  │  │                                                    │  │   │
│  │  │   ┌─────────┐  ┌─────────┐  ┌─────────┐            │  │   │
│  │  │   │ Images  │  │Containers│  │ Volumes │           │  │   │
│  │  │   └─────────┘  └─────────┘  └─────────┘            │  │   │
│  │  │                                                    │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Docker Hub (Cloud)                     │   │
│  │              hub.docker.com - Image Registry             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. ความต้องการของระบบ

### ✅ Hardware Requirements

| รายการ | ความต้องการขั้นต่ำ | แนะนำ |
|--------|-------------------|-------|
| **CPU** | 64-bit with SLAT | Intel Core i5+ / AMD Ryzen 5+ |
| **RAM** | 4 GB | 8 GB+ |
| **Storage** | 20 GB free | 50 GB+ SSD |
| **Virtualization** | Enabled in BIOS | ✅ |

### ✅ Software Requirements

| รายการ | Version | หมายเหตุ |
|--------|---------|----------|
| **Windows** | Windows 11 (64-bit) | Home หรือ Pro |
| **WSL** | WSL 2 | ต้องติดตั้งก่อน |
| **VS Code** | Latest | พร้อม WSL Extension |

### ✅ ตรวจสอบ Virtualization

1. กด `Ctrl + Shift + Esc` เปิด Task Manager
2. ไปที่ Tab **Performance** → **CPU**
3. ดูที่ **Virtualization:** ต้องเป็น **Enabled**

```
┌─────────────────────────────────────────┐
│  Task Manager > Performance > CPU       │
├─────────────────────────────────────────┤
│                                         │
│  Virtualization:  Enabled  ✅           │
│                                         │
│  ถ้าเป็น Disabled ต้องเข้า BIOS เปิดก่อน      │
│                                         │
└─────────────────────────────────────────┘
```

---

## 4. ขั้นตอนที่ 1: ตรวจสอบ WSL2

### 4.1 ตรวจสอบว่ามี WSL2 หรือยัง

เปิด **PowerShell** (Run as Administrator) แล้วพิมพ์:

```powershell
wsl --version
```

**ผลลัพธ์ที่ควรเห็น:**
```
WSL version: 2.3.26.0
Kernel version: 5.15.167.4-1
WSLg version: 1.0.65
...
```

### 4.2 ตรวจสอบ Linux Distribution

```powershell
wsl -l -v
```

**ผลลัพธ์ที่ควรเห็น:**
```
  NAME                   STATE           VERSION
* Ubuntu                 Running         2
```

⚠️ **สำคัญ:** VERSION ต้องเป็น **2** (ไม่ใช่ 1)

### 4.3 ถ้ายังไม่มี WSL2 หรือเป็น Version 1

```powershell
# อัพเดท WSL
wsl --update

# ตั้ง Default เป็น WSL2
wsl --set-default-version 2

# ถ้า Ubuntu เป็น Version 1 ให้แปลงเป็น Version 2
wsl --set-version Ubuntu 2
```

### 4.4 ตรวจสอบว่า Ubuntu ทำงานได้

```powershell
# เปิด Ubuntu
wsl
```

ใน Ubuntu ให้ทดสอบ:
```bash
# ตรวจสอบ version
cat /etc/os-release

# ควรเห็น
# NAME="Ubuntu"
# VERSION="22.04.x LTS" หรือใหม่กว่า
```

---

## 5. ขั้นตอนที่ 2: ติดตั้ง Docker Desktop

### 5.1 Download Docker Desktop

1. เปิด Browser ไปที่: **https://www.docker.com/products/docker-desktop/**

2. Click ปุ่ม **"Download for Windows"**

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│         [  Download for Windows - AMD64  ]                      │
│                                                                 │
│         Docker Desktop 4.37.1 (January 2025)                    │
│         ขนาดไฟล์ประมาณ 600 MB                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 ติดตั้ง Docker Desktop

1. เปิดไฟล์ **Docker Desktop Installer.exe** ที่ดาวน์โหลดมา

2. ในหน้า Configuration:
   - ✅ **Use WSL 2 instead of Hyper-V (recommended)** ← ต้องเลือก!
   - ✅ **Add shortcut to desktop**

```
┌─────────────────────────────────────────────────────────────────┐
│            Docker Desktop Installer                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Configuration                                                  │
│                                                                 │
│  [✓] Use WSL 2 instead of Hyper-V (recommended)  ← สำคัญ!        │
│  [✓] Add shortcut to desktop                                    │
│                                                                 │
│                              [ Install ]                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

3. รอการติดตั้ง (ประมาณ 2-5 นาที)

4. Click **"Close and restart"** เพื่อ Restart Windows

### 5.3 เปิด Docker Desktop ครั้งแรก

1. หลังจาก Restart ให้เปิด **Docker Desktop** จาก Desktop หรือ Start Menu

2. ยอมรับ **Terms of Service**

3. เลือก **Use recommended settings** (หรือ Skip)

4. รอจน Docker Engine เริ่มทำงาน (สังเกตไอคอนที่ System Tray)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  System Tray (มุมขวาล่าง)                                         │
│                                                                 │
│  🐳 ← Docker Icon                                               │
│                                                                 │
│  สีเขียว = Running ✅                                             │
│  สีส้ม   = Starting... ⏳                                         │
│  สีแดง   = Error ❌                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. ขั้นตอนที่ 3: ตั้งค่า Docker Desktop

### 6.1 เปิด Settings

Click ไอคอน ⚙️ (Settings) ที่มุมขวาบนของ Docker Desktop

### 6.2 ตั้งค่า Resources → WSL Integration

ไปที่ **Resources** → **WSL Integration**

```
┌─────────────────────────────────────────────────────────────────┐
│  Settings > Resources > WSL Integration                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Enable integration with my default WSL distro    [✓] ON        │
│                                                                 │
│  Enable integration with additional distros:                    │
│                                                                 │
│  Ubuntu                                           [✓] ON ← เปิด! │
│  Ubuntu-22.04                                     [✓] ON        │
│                                                                 │
│                                        [ Apply & restart ]      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**⚠️ สำคัญ:** ต้องเปิด Integration สำหรับ Ubuntu distribution ที่ใช้งาน!

### 6.3 ตั้งค่า Resources (Optional)

ไปที่ **Resources** → **Advanced** (ถ้ามี RAM เพียงพอ)

| Setting | ค่าแนะนำ |
|---------|---------|
| Memory | 4 GB (หรือครึ่งหนึ่งของ RAM) |
| CPU | 2-4 cores |
| Swap | 1 GB |
| Disk image size | 60 GB |

### 6.4 Apply Settings

Click **"Apply & restart"** เพื่อบันทึกการตั้งค่า

---

## 7. ขั้นตอนที่ 4: ตรวจสอบการติดตั้ง

### 7.1 ตรวจสอบจาก PowerShell (Windows)

เปิด **PowerShell** แล้วพิมพ์:

```powershell
docker --version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker version 27.5.0, build a187fa5
```

```powershell
docker compose version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker Compose version v2.32.4
```

### 7.2 ตรวจสอบจาก WSL (Ubuntu)

เปิด **Windows Terminal** → **Ubuntu** หรือพิมพ์ `wsl` ใน PowerShell

```bash
docker --version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker version 27.5.0, build a187fa5
```

```bash
docker compose version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker Compose version v2.32.4
```

### 7.3 ทดสอบรัน Container แรก

ใน WSL (Ubuntu) ให้พิมพ์:

```bash
docker run hello-world
```

**ผลลัพธ์ที่คาดหวัง:**
```
Unable to find image 'hello-world:latest' locally
latest: Pulling from library/hello-world
e6590344b1a5: Pull complete
Digest: sha256:...
Status: Downloaded newer image for hello-world:latest

Hello from Docker!
This message shows that your installation appears to be working correctly.

To generate this message, Docker took the following steps:
 1. The Docker client contacted the Docker daemon.
 2. The Docker daemon pulled the "hello-world" image from the Docker Hub.
 3. The Docker daemon created a new container from that image which runs the
    executable that produces the output you are currently reading.
 4. The Docker daemon streamed that output to the Docker client, which sent it
    to your terminal.
...
```

🎉 **ถ้าเห็นข้อความนี้ แสดงว่า Docker ติดตั้งสำเร็จ!**

---

## 8. ขั้นตอนที่ 5: สมัคร Docker Hub

### 8.1 สมัครบัญชี Docker Hub

1. เปิด Browser ไปที่: **https://hub.docker.com**

2. Click **"Sign Up"**

3. กรอกข้อมูล:
   - **Username:** เลือกชื่อที่จำง่าย (จะใช้เป็น namespace เช่น `sompong/myapp`)
   - **Email:** ใช้ email มหาวิทยาลัย หรือ email ส่วนตัว
   - **Password:** อย่างน้อย 9 ตัวอักษร

4. ยืนยัน Email

5. เลือก Plan: **Personal (Free)** เพียงพอสำหรับการเรียน

```
┌─────────────────────────────────────────────────────────────────┐
│                    Docker Hub - Sign Up                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Username:  [________________________]                          │
│             (ใช้เป็น namespace: username/imagename)               │
│                                                                 │
│  Email:     [________________________]                          │
│                                                                 │
│  Password:  [________________________]                          │
│             (อย่างน้อย 9 ตัวอักษร)                                  │
│                                                                 │
│                      [ Sign Up ]                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 Login จาก Terminal

ใน WSL (Ubuntu) ให้พิมพ์:

```bash
docker login
```

**กรอกข้อมูล:**
```
Username: your-username
Password: ********

Login Succeeded ✅
```

### 8.3 ตรวจสอบการ Login

```bash
docker info | grep Username
```

**ผลลัพธ์:**
```
 Username: your-username
```

---

## 9. แบบฝึกหัด: ทดลองใช้งาน Docker

### 📝 แบบฝึกหัดที่ 1: จัดการ Images

**1.1 ดาวน์โหลด (Pull) Images**

```bash
# Pull Node.js image
docker pull node:20-alpine

# Pull Nginx image
docker pull nginx:alpine

# Pull PostgreSQL image
docker pull postgres:16-alpine
```

**1.2 ดู Images ที่มี**

```bash
docker images
```

**ผลลัพธ์ที่คาดหวัง:**
```
REPOSITORY    TAG         IMAGE ID       CREATED        SIZE
node          20-alpine   abc123def456   2 days ago     135MB
nginx         alpine      def456abc789   1 week ago     43MB
postgres      16-alpine   789abc123def   3 days ago     238MB
hello-world   latest      d2c94e258dcb   8 months ago   13.3kB
```

**📌 สังเกต:** alpine images มีขนาดเล็กกว่า image ปกติมาก

---

### 📝 แบบฝึกหัดที่ 2: รัน Containers

**2.1 รัน Node.js Container**

```bash
# รัน Node.js และดู version
docker run node:20-alpine node --version
```

**ผลลัพธ์:**
```
v20.18.0
```

**2.2 รัน Container แบบ Interactive**

```bash
# เข้าไปใน Container
docker run -it node:20-alpine /bin/sh
```

**ตอนนี้คุณอยู่ "ข้างใน" Container แล้ว!**

```sh
# ลองพิมพ์คำสั่งใน Container
/ # node --version
v20.18.0

/ # npm --version
10.8.2

/ # cat /etc/os-release
NAME="Alpine Linux"
...

/ # exit
```

**2.3 รัน Nginx Web Server**

```bash
# รัน Nginx ใน background
docker run -d -p 8080:80 --name my-nginx nginx:alpine
```

**เปิด Browser ไปที่:** http://localhost:8080

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                Welcome to nginx!                                │
│                                                                 │
│  If you see this page, the nginx web server is successfully     │
│  installed and working.                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

🎉 **คุณเพิ่งรัน Web Server โดยไม่ต้องติดตั้งอะไรเลย!**

---

### 📝 แบบฝึกหัดที่ 3: จัดการ Containers

**3.1 ดู Containers ที่กำลังรัน**

```bash
docker ps
```

**ผลลัพธ์:**
```
CONTAINER ID   IMAGE          COMMAND                  STATUS         PORTS                  NAMES
abc123def456   nginx:alpine   "/docker-entrypoint.…"   Up 5 minutes   0.0.0.0:8080->80/tcp   my-nginx
```

**3.2 ดู Logs ของ Container**

```bash
docker logs my-nginx
```

**3.3 หยุด Container**

```bash
docker stop my-nginx
```

**3.4 ดู Containers ทั้งหมด (รวมที่หยุดแล้ว)**

```bash
docker ps -a
```

**3.5 เริ่ม Container อีกครั้ง**

```bash
docker start my-nginx
```

**3.6 ลบ Container**

```bash
# หยุดก่อน
docker stop my-nginx

# ลบ
docker rm my-nginx
```

---

### 📝 แบบฝึกหัดที่ 4: รัน PostgreSQL Database

**4.1 รัน PostgreSQL Container**

```bash
docker run -d \
  --name my-postgres \
  -e POSTGRES_USER=student \
  -e POSTGRES_PASSWORD=secret123 \
  -e POSTGRES_DB=testdb \
  -p 5432:5432 \
  postgres:16-alpine
```

**4.2 เชื่อมต่อเข้าไปใน Database**

```bash
docker exec -it my-postgres psql -U student -d testdb
```

**ตอนนี้คุณอยู่ใน PostgreSQL แล้ว!**

```sql
-- สร้าง table
CREATE TABLE students (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
);

-- เพิ่มข้อมูล
INSERT INTO students (name, email) VALUES ('สมชาย', 'somchai@example.com');
INSERT INTO students (name, email) VALUES ('สมหญิง', 'somying@example.com');

-- ดูข้อมูล
SELECT * FROM students;

-- ออกจาก psql
\q
```

**ผลลัพธ์:**
```
 id |   name   |        email
----+----------+---------------------
  1 | สมชาย    | somchai@example.com
  2 | สมหญิง   | somying@example.com
```

🎉 **คุณเพิ่งรัน Database Server โดยไม่ต้องติดตั้ง PostgreSQL!**

---

### 📝 แบบฝึกหัดที่ 5: ทำความสะอาด

**5.1 หยุดและลบ Containers ทั้งหมด**

```bash
# หยุดทุก Container
docker stop $(docker ps -q)

# ลบทุก Container
docker rm $(docker ps -aq)
```

**5.2 ลบ Images ที่ไม่ใช้**

```bash
docker image prune -a
```

**5.3 ทำความสะอาดทั้งระบบ**

```bash
docker system prune -a
```

⚠️ **คำสั่งนี้จะลบ:** stopped containers, unused networks, dangling images, build cache

---

## 10. Checklist ก่อนมาเรียน

### ✅ ตรวจสอบว่าทำครบทุกข้อ

| # | รายการ | สถานะ |
|---|--------|-------|
| 1 | WSL2 ติดตั้งและทำงานได้ | ☐ |
| 2 | Docker Desktop ติดตั้งสำเร็จ | ☐ |
| 3 | Docker Engine รันได้ (icon เขียว) | ☐ |
| 4 | WSL Integration เปิดสำหรับ Ubuntu | ☐ |
| 5 | `docker --version` ใน WSL ทำงานได้ | ☐ |
| 6 | `docker run hello-world` สำเร็จ | ☐ |
| 7 | สมัคร Docker Hub แล้ว | ☐ |
| 8 | `docker login` สำเร็จ | ☐ |
| 9 | ทำแบบฝึกหัดครบทุกข้อ | ☐ |

### 📸 Screenshot ที่ต้องเตรียม

เตรียม Screenshot เหล่านี้เพื่อยืนยันการติดตั้ง:

1. **Docker Desktop** - หน้าจอ Dashboard แสดงว่า Engine Running
2. **Terminal** - ผลลัพธ์ของ `docker --version`
3. **Terminal** - ผลลัพธ์ของ `docker run hello-world`
4. **Terminal** - ผลลัพธ์ของ `docker images` (หลังจาก pull images)

---

## 11. การแก้ปัญหาเบื้องต้น

### ❌ ปัญหา: Docker Desktop ไม่เริ่มทำงาน

**อาการ:** Docker icon เป็นสีส้ม/แดง หรือแสดง error

**แก้ไข:**
```powershell
# 1. Restart WSL
wsl --shutdown
wsl

# 2. Restart Docker Desktop
# ปิดแล้วเปิดใหม่

# 3. ตรวจสอบ Virtualization ใน BIOS
# ต้องเปิด Intel VT-x หรือ AMD-V
```

### ❌ ปัญหา: docker command ไม่ทำงานใน WSL

**อาการ:** `docker: command not found`

**แก้ไข:**
1. เปิด Docker Desktop Settings
2. ไปที่ Resources → WSL Integration
3. เปิด Ubuntu distribution
4. Click "Apply & restart"
5. ปิด Terminal แล้วเปิดใหม่

### ❌ ปัญหา: Permission denied

**อาการ:** `permission denied while trying to connect to the Docker daemon socket`

**แก้ไข:**
```bash
# เพิ่ม user เข้ากลุ่ม docker
sudo usermod -aG docker $USER

# Logout แล้ว Login ใหม่
exit
# แล้วเปิด WSL ใหม่
```

### ❌ ปัญหา: Port already in use

**อาการ:** `port is already allocated`

**แก้ไข:**
```bash
# ดูว่า port ถูกใช้โดยอะไร
netstat -ano | findstr :8080

# หรือใช้ port อื่น
docker run -d -p 8081:80 nginx:alpine
```

### ❌ ปัญหา: ไม่สามารถ pull images ได้

**อาการ:** `error pulling image configuration`

**แก้ไข:**
```bash
# ตรวจสอบ internet connection
ping hub.docker.com

# Restart Docker
# Right-click Docker icon > Restart
```

---

## 📚 แหล่งเรียนรู้เพิ่มเติม

- **Docker Official Docs:** https://docs.docker.com/
- **Docker Hub:** https://hub.docker.com/
- **Docker Desktop for Windows:** https://docs.docker.com/desktop/install/windows-install/
- **WSL Documentation:** https://learn.microsoft.com/en-us/windows/wsl/

---

## 🆘 ต้องการความช่วยเหลือ?

หากพบปัญหาที่แก้ไขไม่ได้:

1. **Screenshot** หน้าจอที่เกิด error
2. **บันทึก** ข้อความ error ทั้งหมด
3. **โพสต์** ใน Discord ของวิชา หรือ
4. **ติดต่อ** อาจารย์ผู้สอนในชั่วโมง Office Hours

---

**พร้อมแล้วก็มาเรียน Docker กันเลย! 🐳🚀**

---

*เอกสารนี้จัดทำโดย: นายธนิต เกตุแก้ว*  
*วิชา ENGSE207 สถาปัตยกรรมซอฟต์แวร์*  
*มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา*
