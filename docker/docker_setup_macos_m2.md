# 🐳 คู่มือเตรียมเครื่องสำหรับ Docker
## สำหรับ macOS (Apple Silicon M1/M2/M3) + VS Code

**วิชา:** ENGSE207 สถาปัตยกรรมซอฟต์แวร์  
**มหาวิทยาลัย:** มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา  
**ผู้สอน:** นายธนิต เกตุแก้ว  
**ปรับปรุงล่าสุด:** มกราคม 2569

---

## 📋 สารบัญ

1. [วัตถุประสงค์](#1-วัตถุประสงค์)
2. [Docker คืออะไร?](#2-docker-คืออะไร)
3. [ความต้องการของระบบ](#3-ความต้องการของระบบ)
4. [ขั้นตอนที่ 1: ตรวจสอบ Mac ของคุณ](#4-ขั้นตอนที่-1-ตรวจสอบ-mac-ของคุณ)
5. [ขั้นตอนที่ 2: ติดตั้ง Docker Desktop](#5-ขั้นตอนที่-2-ติดตั้ง-docker-desktop)
6. [ขั้นตอนที่ 3: ตั้งค่า Docker Desktop](#6-ขั้นตอนที่-3-ตั้งค่า-docker-desktop)
7. [ขั้นตอนที่ 4: ตรวจสอบการติดตั้ง](#7-ขั้นตอนที่-4-ตรวจสอบการติดตั้ง)
8. [ขั้นตอนที่ 5: สมัคร Docker Hub](#8-ขั้นตอนที่-5-สมัคร-docker-hub)
9. [แบบฝึกหัด: ทดลองใช้งาน Docker](#9-แบบฝึกหัด-ทดลองใช้งาน-docker)
10. [Checklist ก่อนมาเรียน](#10-checklist-ก่อนมาเรียน)
11. [การแก้ปัญหาเบื้องต้น](#11-การแก้ปัญหาเบื้องต้น)

---

## 1. วัตถุประสงค์

เอกสารนี้จัดทำขึ้นเพื่อให้นักศึกษา:

✅ เข้าใจว่า Docker คืออะไรและทำงานอย่างไร  
✅ ติดตั้ง Docker Desktop บน macOS (Apple Silicon) ได้สำเร็จ  
✅ สมัครบัญชี Docker Hub และ Login ได้  
✅ ทดลองใช้คำสั่ง Docker พื้นฐานได้  
✅ พร้อมสำหรับการเรียน Docker ในชั้นเรียน  

**⏱️ เวลาที่ใช้โดยประมาณ:** 20-30 นาที

---

## 2. Docker คืออะไร?

### 2.1 นิยามอย่างง่าย

**Docker** คือเครื่องมือที่ช่วยให้เรา "แพ็ค" แอปพลิเคชันพร้อมทุกอย่างที่ต้องใช้ (Code, Runtime, Libraries, Settings) ไว้ในกล่องเดียวเรียกว่า **Container** แล้วสามารถนำไปรันได้ทุกที่ที่มี Docker

```
┌─────────────────────────────────────────────────────────────────┐
│                     Docker ทำอะไรได้?                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   📦 BUILD    →   สร้าง Container จาก Application ของเรา         │
│   🚢 SHIP     →   แชร์ Container ผ่าน Docker Hub                  │
│   🏃 RUN      →   รัน Container ได้ทุกเครื่องที่มี Docker               │
│                                                                 │
│   "Build Once, Run Anywhere"                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 ทำไมต้องใช้ Docker?

**ปัญหาแบบเดิม:**
```
👨‍💻 Developer: "Code ทำงานได้ดีบนเครื่อง Mac ผม!"
🖥️ Server:    "แต่มันไม่ทำงานบน Linux Server!"
😱 ทุกคน:      "ทำไม?!"

สาเหตุ:
- Node.js version ไม่ตรงกัน
- Library version ต่างกัน  
- OS settings ต่างกัน
- Architecture ต่างกัน (ARM vs x86)
```

**แก้ปัญหาด้วย Docker:**
```
👨‍💻 Developer: "ผมสร้าง Container บน Mac M2 แล้ว"
🖥️ Server:    "ผม Pull มารันบน Linux... ทำงานได้เลย!"
🎉 ทุกคน:      "เยี่ยม!"

เพราะ:
- ทุกอย่างอยู่ใน Container เดียวกัน
- รันได้เหมือนกันทุกที่
- Docker จัดการ architecture differences ให้
```

### 2.3 คำศัพท์สำคัญ

| คำศัพท์ | ความหมาย | เปรียบเทียบ |
|---------|----------|-------------|
| **Image** | Template สำหรับสร้าง Container (ไม่เปลี่ยนแปลง) | เหมือน Class ใน OOP |
| **Container** | Instance ที่รันจาก Image (รันได้, หยุดได้) | เหมือน Object ใน OOP |
| **Dockerfile** | ไฟล์คำสั่งสำหรับสร้าง Image | เหมือน Recipe |
| **Docker Hub** | ที่เก็บ Images บน Cloud | เหมือน GitHub สำหรับ Code |
| **Registry** | ที่เก็บ Docker Images | เหมือน npm registry |

### 2.4 Docker Architecture บน macOS

```
┌─────────────────────────────────────────────────────────────────┐
│                Docker Architecture on macOS                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Docker Client (CLI)                   │   │
│  │         docker build | docker pull | docker run          │   │
│  │                    (ใช้ใน Terminal)                       │   │
│  └─────────────────────────┬────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                Docker Desktop for Mac                    │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │         Lightweight Linux VM (Apple Hypervisor)    │  │   │
│  │  │                                                    │  │   │
│  │  │   ┌───────────────────────────────────────────┐    │  │   │
│  │  │   │            Docker Engine (ARM64)          │    │  │   │
│  │  │   │                                           │    │  │   │
│  │  │   │  ┌─────────┐ ┌──────────┐ ┌─────────┐     │    │  │   │
│  │  │   │  │ Images  │ │Containers│ │ Volumes │     │    │  │   │
│  │  │   │  └─────────┘ └──────────┘ └─────────┘     │    │  │   │
│  │  │   └───────────────────────────────────────────┘    │  │   │
│  │  │                                                    │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                    Rosetta 2 (for x86 images)            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            │                                    │
│                            ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Docker Hub (Cloud)                     │   │
│  │              hub.docker.com - Image Registry             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.5 Apple Silicon และ Docker

```
┌───────────────────────────────────────────────────────────────────┐
│            Apple Silicon (M1/M2/M3) กับ Docker                     │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Mac ของคุณใช้ชิป Apple Silicon (ARM64 architecture)                 │
│  ซึ่งต่างจาก Intel (x86_64 architecture)                             │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                                                              │ │
│  │   Apple Silicon (M1/M2/M3)          Intel Mac                │ │
│  │   ──────────────────────            ─────────                │ │
│  │   Architecture: ARM64             Architecture: x86_64       │ │
│  │   Native images: linux/arm64      Native images: linux/amd64 │ │
│  │                                                              │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  📌 ส่วนใหญ่ Docker Images รองรับทั้งสอง architectures แล้ว             │
│                                                                   │
│  ✅ Images ที่รองรับ ARM64:                                          │
│     • node:20-alpine                                              │
│     • postgres:16-alpine                                          │
│     • nginx:alpine                                                │
│     • redis:alpine                                                │
│     • python:3.12-alpine                                          │
│                                                                   │
│  ⚠️ ถ้า Image ไม่รองรับ ARM64:                                       │
│     Docker จะใช้ Rosetta 2 แปลงให้อัตโนมัติ                            │
│     (อาจช้ากว่าปกติเล็กน้อย)                                           │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 3. ความต้องการของระบบ

### ✅ Hardware Requirements

| รายการ | ความต้องการ |
|--------|-------------|
| **Mac** | Apple Silicon (M1, M2, M3, M1 Pro, M2 Pro, etc.) |
| **RAM** | 4 GB ขั้นต่ำ (แนะนำ 8 GB+) |
| **Storage** | 20 GB free (แนะนำ 50 GB+) |

### ✅ Software Requirements

| รายการ | Version | หมายเหตุ |
|--------|---------|----------|
| **macOS** | Ventura (13.0) หรือใหม่กว่า | Sonoma (14.x) แนะนำ |
| **Rosetta 2** | Installed | สำหรับ x86 images |
| **VS Code** | Latest | พร้อม Docker Extension |

---

## 4. ขั้นตอนที่ 1: ตรวจสอบ Mac ของคุณ

### 4.1 ตรวจสอบ Chip (Processor)

1. Click **Apple Menu** () ที่มุมซ้ายบน
2. เลือก **About This Mac**

```
┌─────────────────────────────────────────────────────────────────┐
│                       About This Mac                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    macOS Sonoma                                 │
│                    Version 14.3                                 │
│                                                                 │
│   Chip        Apple M2                   ← ต้องเป็น M1/M2/M3      │
│   Memory      16 GB                                             │
│   ...                                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**⚠️ ถ้าเห็น "Processor: Intel Core i..." แสดงว่าเป็น Intel Mac**  
→ ให้ดาวน์โหลด Docker Desktop for Intel แทน

### 4.2 ตรวจสอบ macOS Version

```bash
# เปิด Terminal แล้วพิมพ์
sw_vers
```

**ผลลัพธ์ที่คาดหวัง:**
```
ProductName:        macOS
ProductVersion:     14.3
BuildVersion:       23D56
```

**ต้องการ macOS 13.0 (Ventura) หรือใหม่กว่า**

### 4.3 ติดตั้ง Rosetta 2 (ถ้ายังไม่มี)

Rosetta 2 ช่วยให้รัน x86 images บน Apple Silicon ได้

```bash
# ตรวจสอบว่ามี Rosetta หรือยัง
/usr/bin/pgrep -q oahd && echo "Rosetta 2 is installed" || echo "Rosetta 2 is NOT installed"
```

**ถ้ายังไม่มี ให้ติดตั้ง:**
```bash
softwareupdate --install-rosetta --agree-to-license
```

---

## 5. ขั้นตอนที่ 2: ติดตั้ง Docker Desktop

### 5.1 Download Docker Desktop

1. เปิด Browser ไปที่: **https://www.docker.com/products/docker-desktop/**

2. Click **"Download for Mac"** แล้วเลือก **"Apple Silicon"**

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│         Download for Mac                                        │
│                                                                 │
│         [  Apple Silicon  ]  ← เลือกอันนี้!                         │
│         [  Intel Chip     ]                                     │
│                                                                 │
│         Docker Desktop 4.37.1 (January 2025)                    │
│         ขนาดไฟล์ประมาณ 620 MB                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**⚠️ สำคัญ: ต้องเลือก Apple Silicon ไม่ใช่ Intel Chip!**

### 5.2 ติดตั้ง Docker Desktop

1. เปิดไฟล์ **Docker.dmg** ที่ดาวน์โหลดมา

2. ลาก **Docker icon** ไปใส่ **Applications folder**

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                        Docker.dmg                               │
│                                                                 │
│       ┌─────────┐                    ┌──────────────┐           │
│       │  🐳     │   ────────────►    │ Applications │           │
│       │ Docker  │      Drag here     │    📁        │           │
│       └─────────┘                    └──────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

3. เปิด **Finder** → **Applications** → Double-click **Docker**

4. macOS จะถามว่า "Docker is an app downloaded from the internet. Are you sure you want to open it?"
   → Click **"Open"**

5. กรอก **Password** ของ Mac (ถ้าถูกถาม)

### 5.3 เริ่มต้นใช้งาน Docker Desktop

1. Docker Desktop จะขอ permissions:
   - **Network** - สำหรับ container networking
   - **File System** - สำหรับ volume mounts
   
   → Click **"OK"** ทั้งหมด

2. ยอมรับ **Terms of Service**

3. เลือก **"Use recommended settings"** หรือ **Skip**

4. รอจน Docker Engine เริ่มทำงาน

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  Menu Bar (มุมขวาบน)                                             │
│                                                                 │
│  🐳 ← Docker Icon ใน Menu Bar                                   │
│                                                                 │
│  สีเขียว = Engine Running ✅                                      │
│  กำลังหมุน = Starting... ⏳                                       │
│  สีแดง   = Error ❌                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. ขั้นตอนที่ 3: ตั้งค่า Docker Desktop

### 6.1 เปิด Settings

1. Click 🐳 icon ใน Menu Bar
2. เลือก **Settings** (หรือ Preferences)

หรือกด **⌘ + ,** ใน Docker Desktop window

### 6.2 General Settings

```
┌─────────────────────────────────────────────────────────────────┐
│  Settings > General                                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [✓] Start Docker Desktop when you sign in to your computer     │
│      (เปิด Docker อัตโนมัติเมื่อเปิดเครื่อง - แนะนำ)                     │
│                                                                 │
│  [✓] Show weekly tips                                           │
│                                                                 │
│  [ ] Use Virtualization framework                               │
│      (ปิดไว้ก่อน ถ้าไม่มีปัญหา)                                        │
│                                                                 │
│  [✓] Use Rosetta for x86_64/amd64 emulation on Apple Silicon    │
│      (เปิดไว้ - สำหรับรัน x86 images) ← สำคัญ!                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 Resources Settings

ไปที่ **Resources** → **Advanced**

| Setting | ค่าเริ่มต้น | แนะนำ (ถ้า RAM 16GB) |
|---------|-------------|---------------------|
| CPUs | 4 | 4-6 |
| Memory | 4 GB | 6-8 GB |
| Swap | 1 GB | 1-2 GB |
| Virtual disk limit | 64 GB | 64-100 GB |

```
┌─────────────────────────────────────────────────────────────────┐
│  Settings > Resources > Advanced                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  CPUs:                    [====|=====] 4                        │
│                                                                 │
│  Memory:                  [======|===] 6.00 GB                  │
│                                                                 │
│  Swap:                    [==|=======] 1 GB                     │
│                                                                 │
│  Virtual disk limit:      [======|===] 64 GB                    │
│                                                                 │
│                                        [ Apply & restart ]      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.4 Apply Settings

Click **"Apply & restart"** เพื่อบันทึกการตั้งค่า

---

## 7. ขั้นตอนที่ 4: ตรวจสอบการติดตั้ง

### 7.1 ตรวจสอบจาก Terminal

เปิด **Terminal** (Applications → Utilities → Terminal หรือ Spotlight → Terminal)

```bash
docker --version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker version 27.5.0, build a187fa5
```

```bash
docker compose version
```

**ผลลัพธ์ที่คาดหวัง:**
```
Docker Compose version v2.32.4
```

### 7.2 ตรวจสอบ Architecture

```bash
docker info | grep Architecture
```

**ผลลัพธ์ที่คาดหวัง:**
```
 Architecture: aarch64
```

**aarch64** = ARM64 (Apple Silicon) ✅

### 7.3 ทดสอบรัน Container แรก

```bash
docker run hello-world
```

**ผลลัพธ์ที่คาดหวัง:**
```
Unable to find image 'hello-world:latest' locally
latest: Pulling from library/hello-world
478afc919002: Pull complete
Digest: sha256:...
Status: Downloaded newer image for hello-world:latest

Hello from Docker!
This message shows that your installation appears to be working correctly.

To generate this message, Docker took the following steps:
 1. The Docker client contacted the Docker daemon.
 2. The Docker daemon pulled the "hello-world" image from the Docker Hub.
    (arm64v8)
 3. The Docker daemon created a new container from that image which runs the
    executable that produces the output you are currently reading.
 4. The Docker daemon streamed that output to the Docker client, which sent it
    to your terminal.
...
```

🎉 **ถ้าเห็นข้อความนี้ แสดงว่า Docker ติดตั้งสำเร็จ!**

**สังเกต:** `(arm64v8)` แสดงว่าใช้ native ARM64 image

---

## 8. ขั้นตอนที่ 5: สมัคร Docker Hub

### 8.1 สมัครบัญชี Docker Hub

1. เปิด Browser ไปที่: **https://hub.docker.com**

2. Click **"Sign Up"**

3. กรอกข้อมูล:
   - **Username:** เลือกชื่อที่จำง่าย (จะใช้เป็น namespace เช่น `sompong/myapp`)
   - **Email:** ใช้ email มหาวิทยาลัย หรือ email ส่วนตัว
   - **Password:** อย่างน้อย 9 ตัวอักษร

4. ยืนยัน Email

5. เลือก Plan: **Personal (Free)** เพียงพอสำหรับการเรียน

```
┌─────────────────────────────────────────────────────────────────┐
│                    Docker Hub - Sign Up                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Username:  [________________________]                          │
│             (ใช้เป็น namespace: username/imagename)               │
│                                                                 │
│  Email:     [________________________]                          │
│                                                                 │
│  Password:  [________________________]                          │
│             (อย่างน้อย 9 ตัวอักษร)                                  │
│                                                                 │
│                      [ Sign Up ]                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 Login จาก Terminal

เปิด Terminal แล้วพิมพ์:

```bash
docker login
```

**กรอกข้อมูล:**
```
Username: your-username
Password: ********

Login Succeeded ✅
```

### 8.3 ตรวจสอบการ Login

```bash
docker info | grep Username
```

**ผลลัพธ์:**
```
 Username: your-username
```

### 8.4 Login ผ่าน Docker Desktop (ทางเลือก)

1. เปิด Docker Desktop
2. Click **"Sign in"** ที่มุมขวาบน
3. เลือก **"Sign in with Docker Hub"**
4. Login ผ่าน Browser

---

## 9. แบบฝึกหัด: ทดลองใช้งาน Docker

### 📝 แบบฝึกหัดที่ 1: จัดการ Images

**1.1 ดาวน์โหลด (Pull) Images**

```bash
# Pull Node.js image (ARM64 native)
docker pull node:20-alpine

# Pull Nginx image
docker pull nginx:alpine

# Pull PostgreSQL image
docker pull postgres:16-alpine
```

**สังเกต:** Docker จะดาวน์โหลด ARM64 version โดยอัตโนมัติ

**1.2 ดู Images ที่มี**

```bash
docker images
```

**ผลลัพธ์ที่คาดหวัง:**
```
REPOSITORY    TAG         IMAGE ID       CREATED        SIZE
node          20-alpine   abc123def456   2 days ago     135MB
nginx         alpine      def456abc789   1 week ago     43MB
postgres      16-alpine   789abc123def   3 days ago     238MB
hello-world   latest      d2c94e258dcb   8 months ago   13.3kB
```

**📌 สังเกต:** SIZE ของ alpine images มีขนาดเล็ก

---

### 📝 แบบฝึกหัดที่ 2: รัน Containers

**2.1 รัน Node.js Container**

```bash
# รัน Node.js และดู version
docker run node:20-alpine node --version
```

**ผลลัพธ์:**
```
v20.18.0
```

**2.2 รัน Container แบบ Interactive**

```bash
# เข้าไปใน Container
docker run -it node:20-alpine /bin/sh
```

**ตอนนี้คุณอยู่ "ข้างใน" Container แล้ว!**

```sh
# ลองพิมพ์คำสั่งใน Container
/ # node --version
v20.18.0

/ # npm --version
10.8.2

/ # uname -m
aarch64        # ← ARM64 architecture

/ # cat /etc/os-release
NAME="Alpine Linux"
...

/ # exit
```

**2.3 รัน Nginx Web Server**

```bash
# รัน Nginx ใน background
docker run -d -p 8080:80 --name my-nginx nginx:alpine
```

**เปิด Browser ไปที่:** http://localhost:8080

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                Welcome to nginx!                                │
│                                                                 │
│  If you see this page, the nginx web server is successfully     │
│  installed and working.                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

🎉 **คุณเพิ่งรัน Web Server โดยไม่ต้องติดตั้งอะไรเลย!**

---

### 📝 แบบฝึกหัดที่ 3: จัดการ Containers

**3.1 ดู Containers ที่กำลังรัน**

```bash
docker ps
```

**ผลลัพธ์:**
```
CONTAINER ID   IMAGE          COMMAND                  STATUS         PORTS                  NAMES
abc123def456   nginx:alpine   "/docker-entrypoint.…"   Up 5 minutes   0.0.0.0:8080->80/tcp   my-nginx
```

**3.2 ดู Logs ของ Container**

```bash
docker logs my-nginx
```

**3.3 ดูใน Docker Desktop**

เปิด Docker Desktop → Click ที่ **"Containers"** tab

```
┌─────────────────────────────────────────────────────────────────┐
│  Docker Desktop > Containers                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  NAME        IMAGE          STATUS      PORT(S)        ACTIONS  │
│  my-nginx    nginx:alpine   Running     8080:80        ⏹️ 🗑️    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**3.4 หยุด Container**

```bash
docker stop my-nginx
```

**3.5 เริ่ม Container อีกครั้ง**

```bash
docker start my-nginx
```

**3.6 ลบ Container**

```bash
# หยุดก่อน
docker stop my-nginx

# ลบ
docker rm my-nginx
```

---

### 📝 แบบฝึกหัดที่ 4: รัน PostgreSQL Database

**4.1 รัน PostgreSQL Container**

```bash
docker run -d \
  --name my-postgres \
  -e POSTGRES_USER=student \
  -e POSTGRES_PASSWORD=secret123 \
  -e POSTGRES_DB=testdb \
  -p 5432:5432 \
  postgres:16-alpine
```

**4.2 เชื่อมต่อเข้าไปใน Database**

```bash
docker exec -it my-postgres psql -U student -d testdb
```

**ตอนนี้คุณอยู่ใน PostgreSQL แล้ว!**

```sql
-- สร้าง table
CREATE TABLE students (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
);

-- เพิ่มข้อมูล
INSERT INTO students (name, email) VALUES ('สมชาย', 'somchai@example.com');
INSERT INTO students (name, email) VALUES ('สมหญิง', 'somying@example.com');

-- ดูข้อมูล
SELECT * FROM students;

-- ออกจาก psql
\q
```

**ผลลัพธ์:**
```
 id |   name   |        email
----+----------+---------------------
  1 | สมชาย    | somchai@example.com
  2 | สมหญิง   | somying@example.com
```

🎉 **คุณเพิ่งรัน Database Server โดยไม่ต้องติดตั้ง PostgreSQL!**

---

### 📝 แบบฝึกหัดที่ 5: ทดสอบ Multi-Platform Image

**5.1 ตรวจสอบว่า Image รองรับ ARM64 หรือไม่**

```bash
docker manifest inspect node:20-alpine | grep architecture
```

**ผลลัพธ์:**
```
"architecture": "amd64",
"architecture": "arm64",
...
```

**ถ้าเห็น `arm64` แสดงว่ารองรับ Apple Silicon native**

**5.2 บังคับใช้ Platform เฉพาะ (สำหรับทดสอบ)**

```bash
# รัน ARM64 image (native - เร็ว)
docker run --platform linux/arm64 node:20-alpine node --version

# รัน AMD64 image (ผ่าน Rosetta - ช้ากว่า)
docker run --platform linux/amd64 node:20-alpine node --version
```

**📌 หมายเหตุ:** ปกติไม่ต้องระบุ `--platform` Docker จะเลือกให้อัตโนมัติ

---

### 📝 แบบฝึกหัดที่ 6: ทำความสะอาด

**6.1 หยุดและลบ Containers ทั้งหมด**

```bash
# หยุดทุก Container
docker stop $(docker ps -q)

# ลบทุก Container
docker rm $(docker ps -aq)
```

**6.2 ลบ Images ที่ไม่ใช้**

```bash
docker image prune -a
```

**6.3 ทำความสะอาดทั้งระบบ**

```bash
docker system prune -a
```

**หรือใช้ Docker Desktop:**
1. Click 🐳 icon ใน Menu Bar
2. เลือก **"Troubleshoot"** (🔧)
3. Click **"Clean / Purge data"**

---

## 10. Checklist ก่อนมาเรียน

### ✅ ตรวจสอบว่าทำครบทุกข้อ

| # | รายการ | สถานะ |
|---|--------|-------|
| 1 | Mac เป็น Apple Silicon (M1/M2/M3) | ☐ |
| 2 | macOS 13.0+ (Ventura/Sonoma) | ☐ |
| 3 | Rosetta 2 ติดตั้งแล้ว | ☐ |
| 4 | Docker Desktop ติดตั้งสำเร็จ | ☐ |
| 5 | Docker Engine รันได้ (icon เขียว) | ☐ |
| 6 | `docker --version` ทำงานได้ | ☐ |
| 7 | `docker run hello-world` สำเร็จ | ☐ |
| 8 | สมัคร Docker Hub แล้ว | ☐ |
| 9 | `docker login` สำเร็จ | ☐ |
| 10 | ทำแบบฝึกหัดครบทุกข้อ | ☐ |

### 📸 Screenshot ที่ต้องเตรียม

เตรียม Screenshot เหล่านี้เพื่อยืนยันการติดตั้ง:

1. **About This Mac** - แสดง Chip เป็น Apple M1/M2/M3
2. **Docker Desktop** - หน้าจอ Dashboard แสดงว่า Engine Running
3. **Terminal** - ผลลัพธ์ของ `docker --version`
4. **Terminal** - ผลลัพธ์ของ `docker run hello-world`
5. **Terminal** - ผลลัพธ์ของ `docker images` (หลังจาก pull images)

---

## 11. การแก้ปัญหาเบื้องต้น

### ❌ ปัญหา: Docker Desktop ไม่เริ่มทำงาน

**อาการ:** Docker icon หมุนอยู่นาน หรือแสดง error

**แก้ไข:**
```bash
# 1. Quit Docker Desktop แล้วเปิดใหม่
# Click 🐳 → Quit Docker Desktop

# 2. ถ้ายังไม่ได้ ให้ลบ cache
rm -rf ~/Library/Containers/com.docker.docker
rm -rf ~/.docker

# 3. เปิด Docker Desktop ใหม่
```

### ❌ ปัญหา: "Cannot connect to the Docker daemon"

**อาการ:** `docker: Cannot connect to the Docker daemon at unix:///var/run/docker.sock`

**แก้ไข:**
1. ตรวจสอบว่า Docker Desktop เปิดอยู่
2. รอให้ Engine เริ่มทำงาน (icon เป็นสีเขียว)
3. ถ้ายังไม่ได้ ให้ Restart Docker Desktop

### ❌ ปัญหา: Image pull ช้ามาก

**อาการ:** Pulling image ใช้เวลานาน

**แก้ไข:**
```bash
# ตรวจสอบ internet connection
ping hub.docker.com

# ใช้ mirror (ถ้าต้องการ)
# เปิด Docker Desktop → Settings → Docker Engine
# เพิ่ม:
{
  "registry-mirrors": ["https://mirror.gcr.io"]
}
```

### ❌ ปัญหา: "no matching manifest for linux/arm64"

**อาการ:** Image ไม่รองรับ ARM64

**แก้ไข:**
```bash
# ใช้ Rosetta emulation
docker run --platform linux/amd64 your-image:tag

# หรือหา ARM64 alternative
# เช่น แทนที่จะใช้ some-image:latest
# ใช้ some-image:latest-arm64 หรือ some-image:alpine (มักรองรับ ARM64)
```

### ❌ ปัญหา: Container ทำงานช้า

**อาการ:** Container รันช้ากว่าที่คาดหวัง

**สาเหตุ & แก้ไข:**
```bash
# 1. ตรวจสอบว่าใช้ ARM64 image หรือไม่
docker inspect your-container | grep Architecture

# ถ้าเป็น amd64 → ใช้ Rosetta → ช้ากว่า native

# 2. เพิ่ม Resources ใน Docker Desktop
# Settings → Resources → Advanced → เพิ่ม Memory/CPU

# 3. ใช้ ARM64 native image
docker pull --platform linux/arm64 node:20-alpine
```

### ❌ ปัญหา: Port already in use

**อาการ:** `port is already allocated`

**แก้ไข:**
```bash
# ดูว่า port ถูกใช้โดยอะไร
lsof -i :8080

# Kill process ที่ใช้ port นั้น
kill -9 <PID>

# หรือใช้ port อื่น
docker run -d -p 8081:80 nginx:alpine
```

### ❌ ปัญหา: "Permission denied" สำหรับ Volume

**อาการ:** ไม่สามารถ mount volume ได้

**แก้ไข:**
1. เปิด **System Settings** → **Privacy & Security** → **Files and Folders**
2. ให้ **Docker** เข้าถึง folders ที่ต้องการ
3. หรือเปิด Docker Desktop → Settings → Resources → File Sharing → เพิ่ม folder

---

## 📚 แหล่งเรียนรู้เพิ่มเติม

- **Docker Official Docs:** https://docs.docker.com/
- **Docker Hub:** https://hub.docker.com/
- **Docker Desktop for Mac:** https://docs.docker.com/desktop/install/mac-install/
- **Apple Silicon Docker FAQ:** https://docs.docker.com/desktop/mac/apple-silicon/

---

## 🆘 ต้องการความช่วยเหลือ?

หากพบปัญหาที่แก้ไขไม่ได้:

1. **Screenshot** หน้าจอที่เกิด error
2. **บันทึก** ข้อความ error ทั้งหมด
3. **รันคำสั่งนี้** แล้วบันทึกผลลัพธ์:
   ```bash
   docker info
   docker version
   uname -a
   sw_vers
   ```
4. **โพสต์** ใน Discord ของวิชา หรือ
5. **ติดต่อ** อาจารย์ผู้สอนในชั่วโมง Office Hours

---

**พร้อมแล้วก็มาเรียน Docker กันเลย! 🐳🍎**

---

*เอกสารนี้จัดทำโดย: นายธนิต เกตุแก้ว*  
*วิชา ENGSE207 สถาปัตยกรรมซอฟต์แวร์*  
*มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา*
