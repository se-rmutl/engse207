# 📐 WORKSHOP ARCHITECTURE DESIGN GUIDE
## แนวทางการออกแบบ Architecture สำหรับ Workshop

---

## 📋 ภาพรวม

เอกสารนี้เป็นแนวทางการสร้าง `ARCHITECTURE.md` สำหรับ **Product Management System Workshop**

**สำหรับ:** Workshop ก่อนสอบ Midterm - ฝึก Refactoring  
**ระบบ:** Product Management (จัดการสินค้า)  
**เวลา:** 60 นาที

---

## 🎯 วัตถุประสงค์

1. เรียนรู้วิธีออกแบบ Architecture Diagram
2. เข้าใจโครงสร้าง Layered Architecture
3. ฝึกสร้าง ARCHITECTURE.md จริง
4. เตรียมพร้อมสำหรับข้อสอบ

---

## 📊 PART 1: C1 Context Diagram

### **วัตถุประสงค์:**
แสดงภาพรวมระบบ - ใครใช้? ระบบทำอะไร?

### **Diagram:**

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│                    System User                      │
│         (เจ้าของร้าน, พนักงาน, ผู้จัดการ)                 │
│                                                     │
└────────────┬────────────────────────────────────────┘
             │
             │ HTTP/JSON
             │ (CRUD Operations)
             │
             ▼
┌─────────────────────────────────────────────────────┐
│                                                     │
│       Product Management System                     │
│                                                     │
│  • จัดการข้อมูลสินค้า (CRUD)                             │
│  • คำนวณมูลค่ารวมของสินค้า                              │
│  • กรองสินค้าตาม Category                             │
│  • แสดงสถิติสินค้า                                      │
│                                                     │
└────────────┬────────────────────────────────────────┘
             │
             │ SQL Queries
             │
             ▼
┌─────────────────────────────────────────────────────┐
│                                                     │
│              SQLite Database                        │
│               (products.db)                         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### **คำอธิบาย:**

**1. Actors (ผู้ใช้งาน):**
- **System User** - เจ้าของร้าน, พนักงาน, ผู้จัดการที่ใช้ระบบ

**2. System (ระบบ):**
- **Product Management System** - ระบบจัดการสินค้า
  - จัดการข้อมูลสินค้า (เพิ่ม/แก้ไข/ลบ)
  - คำนวณมูลค่ารวม (price × stock)
  - กรองสินค้าตาม category
  - แสดงสถิติและรายงาน

**3. External Systems (ระบบภายนอก):**
- **SQLite Database** - ฐานข้อมูลเก็บข้อมูลสินค้า

---

## 🏗️ PART 2: C2 Container Diagram (Layered Architecture)

### **วัตถุประสงค์:**
แสดงโครงสร้างภายในระบบ - แบ่ง Layers อย่างไร?

### **Diagram:**

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT (Browser)                         │
│                     Web UI / HTTP Client                        │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ HTTP/JSON
             │ (GET, POST, PUT, DELETE)
             │
             ▼
╔════════════════════════════════════════════════════════════════╗
║                  PRODUCT MANAGEMENT SYSTEM                     ║
║                     (Express.js Application)                   ║
╠════════════════════════════════════════════════════════════════╣
║                                                                ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           📋 PRESENTATION LAYER                           │ ║
║  │                                                           │ ║
║  │  • Routes        (productRoutes.js)                       │ ║
║  │    - กำหนด API endpoints                                  │ ║
║  │    - Routing requests                                     │ ║
║  │                                                           │ ║
║  │  • Controllers   (productController.js)                   │ ║
║  │    - รับ HTTP Request                                      │ ║
║  │    - ส่ง HTTP Response                                     │ ║
║  │    - Error Handling                                       │ ║
║  │                                                           │ ║
║  │  • Middlewares   (errorHandler.js)                        │ ║
║  │    - Global error handling                                │ ║
║  │    - Logging                                              │ ║
║  │                                                           │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         │ JavaScript Objects                   ║
║                         │ { name, price, stock, category }     ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           🧠 BUSINESS LOGIC LAYER                         │ ║
║  │                                                           │ ║
║  │  • Services      (productService.js)                      │ ║
║  │    - Business rules                                       │ ║
║  │    - Orchestration logic                                  │ ║
║  │    - Calculate total value                                │ ║
║  │                                                           │ ║
║  │  • Validators    (productValidator.js)                    │ ║
║  │    - Input validation                                     │ ║
║  │    - Required fields check                                │ ║
║  │    - Price validation (> 0)                               │ ║
║  │                                                           │ ║
║  │  Business Rules:                                          │ ║
║  │    ✓ Name, price, category required                       │ ║
║  │    ✓ Price must be > 0                                    │ ║
║  │    ✓ Stock must be >= 0                                   │ ║
║  │    ✓ Calculate: totalValue = Σ(price × stock)             │ ║
║  │                                                           │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         │ Data Objects                         ║
║                         │ { id, name, price, stock, category } ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           💾 DATA ACCESS LAYER                            │ ║
║  │                                                           │ ║
║  │  • Repositories  (productRepository.js)                   │ ║
║  │    - CRUD operations                                      │ ║
║  │    - SQL query execution                                  │ ║
║  │    - Data mapping                                         │ ║
║  │                                                           │ ║
║  │  • Database      (connection.js)                          │ ║
║  │    - Database connection                                  │ ║
║  │    - Initialize tables                                    │ ║
║  │                                                           │ ║
║  │  Methods:                                                 │ ║
║  │    • findAll(category)                                    │ ║
║  │    • findById(id)                                         │ ║
║  │    • create(productData)                                  │ ║
║  │    • update(id, productData)                              │ ║
║  │    • delete(id)                                           │ ║
║  │                                                           │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         │ SQL Queries                          ║
║                         │ SELECT, INSERT, UPDATE, DELETE       ║
║                         │                                      ║
╚═════════════════════════╪══════════════════════════════════════╝
                          │
                          ▼
              ┌─────────────────────────┐
              │    SQLite Database      │
              │     (products.db)       │
              │                         │
              │  Table: products        │
              │  - id                   │
              │  - name                 │
              │  - price                │
              │  - stock                │
              │  - category             │
              │  - created_at           │
              └─────────────────────────┘
```

---

## 📝 PART 3: Responsibilities ของแต่ละ Layer

### **1️⃣ Presentation Layer (การนำเสนอ)**

**หน้าที่:**
- รับ HTTP Request จาก Client
- แปลง Request เป็น Data ที่ Business Layer เข้าใจ
- เรียกใช้ Business Layer
- แปลงผลลัพธ์เป็น HTTP Response
- จัดการ Error และส่ง Error Response

**ไฟล์:**
- `productRoutes.js` - กำหนด API endpoints
- `productController.js` - จัดการ HTTP Request/Response
- `errorHandler.js` - จัดการ Error แบบรวมศูนย์

**ตัวอย่างโค้ด:**
```javascript
// productController.js
async getAllProducts(req, res, next) {
    try {
        const { category } = req.query;
        
        // เรียกใช้ Business Layer
        const result = await productService.getAllProducts(category);
        
        // ส่ง Response
        res.json(result);
    } catch (error) {
        // ส่งต่อไปยัง Error Handler
        next(error);
    }
}
```

**สิ่งที่ห้ามทำ:**
- ❌ เขียน SQL Query โดยตรง
- ❌ เขียน Business Logic
- ❌ เข้าถึง Database โดยตรง

---

### **2️⃣ Business Logic Layer (ตัวตนของระบบ)**

**หน้าที่:**
- ตรวจสอบความถูกต้องของข้อมูล (Validation)
- บังคับใช้ Business Rules
- คำนวณและประมวลผลข้อมูล
- เรียกใช้ Data Layer เมื่อต้องการข้อมูล

**ไฟล์:**
- `productService.js` - Business logic หลัก
- `productValidator.js` - Validation rules

**ตัวอย่างโค้ด:**
```javascript
// productService.js
async createProduct(productData) {
    // 1. Validation
    productValidator.validateProductData(productData);
    productValidator.validatePrice(productData.price);
    
    // 2. Create via repository
    const product = await productRepository.create(productData);
    
    return product;
}

async getAllProducts(category) {
    // 1. Get products from repository
    const products = await productRepository.findAll(category);
    
    // 2. Calculate total value (Business Logic)
    const totalValue = products.reduce((sum, p) => 
        sum + (p.price * p.stock), 0
    );
    
    return {
        products,
        totalValue: totalValue.toFixed(2)
    };
}
```

**Business Rules สำคัญ:**
1. ✅ **Required fields: name, price, category**
   ```javascript
   function validateProductData(data) {
       if (!data.name || !data.price || !data.category) {
           throw new Error('Name, price, and category are required');
       }
   }
   ```

2. ✅ **Price must be > 0**
   ```javascript
   function validatePrice(price) {
       if (price <= 0) {
           throw new Error('Price must be greater than 0');
       }
   }
   ```

3. ✅ **Stock must be >= 0**
   ```javascript
   function validateStock(stock) {
       if (stock < 0) {
           throw new Error('Stock cannot be negative');
       }
   }
   ```

4. ✅ **Calculate total value**
   ```javascript
   const totalValue = products.reduce((sum, p) => 
       sum + (p.price * p.stock), 0
   );
   ```

**สิ่งที่ห้ามทำ:**
- ❌ เขียน SQL Query
- ❌ จัดการ HTTP Request/Response
- ❌ เข้าถึง Database โดยตรง

---

### **3️⃣ Data Access Layer (ประตูสู่ฐานข้อมูล)**

**หน้าที่:**
- เชื่อมต่อกับ Database
- ทำ CRUD Operations (Create, Read, Update, Delete)
- แปลง SQL Result เป็น JavaScript Objects
- จัดการ Database Errors

**ไฟล์:**
- `productRepository.js` - CRUD operations
- `connection.js` - Database connection

**ตัวอย่างโค้ด:**
```javascript
// productRepository.js
async function findAll(category = null) {
    return new Promise((resolve, reject) => {
        let sql = 'SELECT * FROM products';
        const params = [];
        
        if (category) {
            sql += ' WHERE category = ?';
            params.push(category);
        }
        
        db.all(sql, params, (err, rows) => {
            if (err) reject(err);
            else resolve(rows);
        });
    });
}
```

**Methods ที่ต้องมี:**
- `findAll(category)` - ดึงสินค้าทั้งหมด
- `findById(id)` - ดึงสินค้าตาม ID
- `create(productData)` - สร้างสินค้าใหม่
- `update(id, productData)` - แก้ไขสินค้า
- `delete(id)` - ลบสินค้า

**สิ่งที่ห้ามทำ:**
- ❌ เขียน Business Logic
- ❌ เขียน Validation
- ❌ จัดการ HTTP Request/Response

---

## 🔄 PART 4: Data Flow (Request → Response)

### **ตัวอย่าง: Create Product**

```
┌──────────────────────────────────────────────────────────────┐
│ 1. CLIENT                                                    │
│    POST /api/products                                        │
│    Body: {                                                   │
│      "name": "Laptop",                                       │
│      "price": 25000,                                         │
│      "stock": 10,                                            │
│      "category": "Electronics"                               │
│    }                                                         │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. PRESENTATION LAYER                                        │
│                                                              │
│    Routes (productRoutes.js)                                │
│    ├─ router.post('/', ...)                                 │
│    │                                                         │
│    Controller (productController.js)                        │
│    ├─ createProduct(req, res, next)                         │
│    ├─ Parse productData from req.body                       │
│    └─ Call: productService.createProduct(productData)       │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. BUSINESS LOGIC LAYER                                     │
│                                                             │
│    Service (productService.js)                              │
│    ├─ createProduct(productData)                            │
│    │                                                        │
│    ├─ ✅ Validate:                                          │
│    │    validator.validateProductData(data)                 │
│    │    - Check name, price, category required              │
│    │                                                        │
│    │    validator.validatePrice(price)                      │
│    │    - Check price > 0                                   │
│    │                                                        │
│    └─ Call: productRepository.create(productData)           │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. DATA ACCESS LAYER                                        │
│                                                             │
│    Repository (productRepository.js)                        │
│    ├─ create(productData)                                   │
│    ├─ Execute SQL:                                          │
│    │   INSERT INTO products                                 │
│    │   (name, price, stock, category)                       │
│    │   VALUES ('Laptop', 25000, 10, 'Electronics')          │
│    │                                                        │
│    └─ Return: { id: 1, name: 'Laptop', ... }                │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. DATABASE                                                 │
│    SQLite (products.db)                                     │
│    ├─ Execute INSERT query                                  │
│    └─ Return lastID                                         │
└────────────┬────────────────────────────────────────────────┘
             │
             │ (Return back through layers)
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. BUSINESS LOGIC LAYER                                     │
│    Service returns created product                          │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. PRESENTATION LAYER                                       │
│    Controller (productController.js)                        │
│    ├─ Receive created product                               │
│    └─ res.status(201).json(product)                         │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. CLIENT                                                   │
│    Response: 201 Created                                    │
│    {                                                        │
│      "id": 1,                                               │
│      "name": "Laptop",                                      │
│      "price": 25000,                                        │
│      "stock": 10,                                           │
│      "category": "Electronics"                              │
│    }                                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 PART 5: Template ARCHITECTURE.md

**Copy template ด้านล่างไปใช้ได้เลย!**

```markdown
# Product Management System - Architecture

## C1: System Context Diagram

\`\`\`
┌─────────────────────────────────────────────────────┐
│                    System User                      │
│         (เจ้าของร้าน, พนักงาน, ผู้จัดการ)                 │
└────────────┬────────────────────────────────────────┘
             │ HTTP/JSON (CRUD Operations)
             ▼
┌─────────────────────────────────────────────────────┐
│       Product Management System                     │
│  • จัดการข้อมูลสินค้า (CRUD)                             │
│  • คำนวณมูลค่ารวมของสินค้า                              │
│  • กรองสินค้าตาม Category                             │
└────────────┬────────────────────────────────────────┘
             │ SQL Queries
             ▼
┌─────────────────────────────────────────────────────┐
│              SQLite Database                        │
│               (products.db)                         │
└─────────────────────────────────────────────────────┘
\`\`\`

### Actors
- **System User**: เจ้าของร้าน, พนักงาน, ผู้จัดการที่ใช้ระบบจัดการสินค้า

### System
- **Product Management System**: ระบบจัดการสินค้า
  - จัดการข้อมูลสินค้า (CRUD)
  - คำนวณมูลค่ารวม (price × stock)
  - กรองสินค้าตาม category

### External Systems
- **SQLite Database**: ฐานข้อมูลเก็บข้อมูลสินค้า

---

## C2: Container Diagram - Layered Architecture

\`\`\`
┌────────────────────────────────────────────────────────────────┐
│                        CLIENT (Browser)                        │
└────────────┬───────────────────────────────────────────────────┘
             │ HTTP/JSON
             ▼
╔════════════════════════════════════════════════════════════════╗
║                  PRODUCT MANAGEMENT SYSTEM                     ║
╠════════════════════════════════════════════════════════════════╣
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           📋 PRESENTATION LAYER                           │ ║
║  │  • Routes (productRoutes.js)                              │ ║
║  │  • Controllers (productController.js)                     │ ║
║  │  • Middlewares (errorHandler.js)                          │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           🧠 BUSINESS LOGIC LAYER                         │ ║
║  │  • Services (productService.js)                           │ ║
║  │  • Validators (productValidator.js)                       │ ║
║  │                                                           │ ║
║  │  Business Rules:                                          │ ║
║  │    ✓ Name, price, category required                       │ ║
║  │    ✓ Price must be > 0                                    │ ║
║  │    ✓ Calculate totalValue = Σ(price × stock)              │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
║                         │                                      ║
║                         ▼                                      ║
║  ┌───────────────────────────────────────────────────────────┐ ║
║  │           💾 DATA ACCESS LAYER                            │ ║
║  │  • Repositories (productRepository.js)                    │ ║
║  │  • Database (connection.js)                               │ ║
║  │                                                           │ ║
║  │  Methods:                                                 │ ║
║  │    • findAll(category)                                    │ ║
║  │    • findById(id)                                         │ ║
║  │    • create(productData)                                  │ ║
║  │    • update(id, productData)                              │ ║
║  │    • delete(id)                                           │ ║
║  └──────────────────────┬────────────────────────────────────┘ ║
╚═════════════════════════╪══════════════════════════════════════╝
                          │ SQL
                          ▼
              ┌─────────────────────────┐
              │    SQLite Database      │
              │     (products.db)       │
              │                         │
              │  Table: products        │
              │  - id                   │
              │  - name                 │
              │  - price                │
              │  - stock                │
              │  - category             │
              │  - created_at           │
              └─────────────────────────┘
\`\`\`

---

## Layer Responsibilities

### 1. Presentation Layer
**หน้าที่:**
- รับ HTTP Request
- Parse parameters
- เรียก Service
- ส่ง HTTP Response

**ไฟล์:**
- `productRoutes.js` - API endpoints
- `productController.js` - HTTP handling
- `errorHandler.js` - Error handling

**ห้ามทำ:**
- ❌ เขียน SQL Query
- ❌ เขียน Business Logic

---

### 2. Business Logic Layer
**หน้าที่:**
- Validation
- Business Rules
- Calculations
- เรียก Repository

**ไฟล์:**
- `productService.js` - Business logic
- `productValidator.js` - Validation

**Business Rules:**
- Name, price, category required
- Price > 0
- Calculate: totalValue = Σ(price × stock)

**ห้ามทำ:**
- ❌ เขียน SQL Query
- ❌ จัดการ HTTP

---

### 3. Data Access Layer
**หน้าที่:**
- CRUD Operations
- Execute SQL
- Return data

**ไฟล์:**
- `productRepository.js` - CRUD
- `connection.js` - DB connection

**Methods:**
- findAll(category)
- findById(id)
- create(productData)
- update(id, data)
- delete(id)

**ห้ามทำ:**
- ❌ Business Logic
- ❌ Validation

---

## Data Flow: Create Product

\`\`\`
Client → Controller → Service → Repository → Database
         ↓ parse     ↓ validate  ↓ SQL       ↓ insert
         ← response ← ← ← ← ← ← ← ← ← ←
\`\`\`

**Steps:**
1. Client ส่ง POST request พร้อมข้อมูลสินค้า
2. Controller parse request → เรียก Service
3. Service validate data → เรียก Repository
4. Repository execute SQL INSERT
5. Database insert record
6. Response กลับไปยัง Client

---

## Summary

### Architecture Benefits
✅ **Separation of Concerns** - แยก layer ชัดเจน  
✅ **Maintainability** - แก้ไขง่าย  
✅ **Testability** - Test แยก layer ได้  
✅ **Scalability** - ขยายง่าย  

### Key Principles
- Each layer has clear responsibilities
- Dependencies flow downward
- No layer skipping
- Business rules stay in Business Layer
\`\`\`
```
---

## ✅ Checklist ก่อนส่ง

### ความครบถ้วน:
- [ ] มี C1 Context Diagram
- [ ] มี C2 Container Diagram
- [ ] อธิบาย Responsibilities ครบ 3 layers
- [ ] มี Data Flow

### คุณภาพ:
- [ ] Diagram ชัดเจน อ่านง่าย
- [ ] ใช้ ASCII art หรือ Markdown
- [ ] อธิบายละเอียด เข้าใจง่าย

### เนื้อหา:
- [ ] แสดง 3 layers ชัดเจน
- [ ] ระบุไฟล์ใน layer ถูกต้อง
- [ ] Business rules ครบถ้วน

---

## 💡 Tips

### ✅ **ควรทำ:**
- ใช้ ASCII art (copy-paste ได้)
- อธิบายชัดเจน กระชับ
- ยกตัวอย่างโค้ดประกอบ

### ❌ **ไม่ควรทำ:**
- วาด diagram มือ
- ใช้รูปภาพ
- อธิบายยาวเกินไป

---

**จัดทำโดย:** อาจารย์ธนิต เกตุแก้ว  
**วิชา:** ENGSE207 สถาปัตยกรรมซอฟต์แวร์  
**สำหรับ:** Workshop - Product Management System
