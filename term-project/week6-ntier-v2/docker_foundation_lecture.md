# 🐳 Docker Foundation
## พื้นฐาน Docker สำหรับนักศึกษาวิศวกรรมซอฟต์แวร์

**รายวิชา:** ENGSE207 Software Architecture  
**เนื้อหา:** บทนำสู่ Docker และ Containerization  
**ระยะเวลาการบรรยาย:** 2-3 ชั่วโมง  
**ระดับความยาก:** เริ่มต้น → กลาง

---

## 📋 สารบัญ

1. [ทำไมต้องเรียน Docker?](#1-ทำไมต้องเรียน-docker)
2. [จาก VM สู่ Container: ปัญหาที่ต้องแก้](#2-จาก-vm-สู่-container-ปัญหาที่ต้องแก้)
3. [Docker คืออะไร?](#3-docker-คืออะไร)
4. [Docker Concepts พื้นฐาน](#4-docker-concepts-พื้นฐาน)
5. [ติดตั้งและตรวจสอบ Docker](#5-ติดตั้งและตรวจสอบ-docker)
6. [Docker Hub และ Registry](#6-docker-hub-และ-registry)
7. [การใช้งาน Docker Commands พื้นฐาน](#7-การใช้งาน-docker-commands-พื้นฐาน)
8. [สร้าง Dockerfile แรกของคุณ](#8-สร้าง-dockerfile-แรกของคุณ)
9. [Docker Compose: จัดการหลาย Containers](#9-docker-compose-จัดการหลาย-containers)
10. [Best Practices และข้อควรระวัง](#10-best-practices-และข้อควรระวัง)
11. [เตรียมพร้อมสำหรับ Lab](#11-เตรียมพร้อมสำหรับ-lab)

---

## 1. ทำไมต้องเรียน Docker?

### 🎯 ความสำคัญของ Docker ในอุตสาหกรรม

Docker เป็นเครื่องมือที่ **นักพัฒนาซอฟต์แวร์ทุกคนต้องรู้** ในปัจจุบัน เพราะ:

**1. Industry Standard**
- บริษัทเทคโนโลยีชั้นนำใช้ Docker (Google, Netflix, LINE, Shopee)
- Required skill ในงานสาย DevOps, Backend, Full-stack

**2. Cloud-Native Development**
- AWS, GCP, Azure ล้วนรองรับ Docker
- พื้นฐานสำหรับ Kubernetes (Container Orchestration)

**3. ใน Course ENGSE207**
- Week 6: N-Tier Architecture บน Docker
- Week 7: Cloud & Containerization
- Term Project: Deploy ด้วย Docker Compose

### 📊 Docker Adoption ในประเทศไทย

```
แอปพลิเคชันที่ใช้ Docker/Container:
┌────────────────────────────────────────────────────────┐
│  LINE         - Messaging, LINE MAN, LINE Shopping     │
│  Shopee       - E-commerce platform                    │
│  หมอพร้อม      - Healthcare application                 │
│  กรุงไทย NEXT  - Banking application                    │
│  True Digital - Streaming, TrueID                      │
│  Grab         - Super app                              │
└────────────────────────────────────────────────────────┘
```

---

## 2. จาก VM สู่ Container: ปัญหาที่ต้องแก้

### 📌 ทบทวน: สถาปัตยกรรม N-Tier บน Single VM

ในสัปดาห์ที่ผ่านมา เราได้ deploy ระบบ Task Board บน Single VM:

```
╔════════════════════════════════════════════════════════════════╗
║                    Ubuntu VM (Single Machine)                  ║
║  ┌──────────────────────────────────────────────────────────┐  ║
║  │  Nginx (apt install nginx)                               │  ║
║  │  - Port 80/443                                           │  ║
║  │  - Config: /etc/nginx/sites-available/                   │  ║
║  └──────────────────────────────────────────────────────────┘  ║
║  ┌──────────────────────────────────────────────────────────┐  ║
║  │  Node.js + PM2                                           │  ║
║  │  - nvm install 20                                        │  ║
║  │  - npm install -g pm2                                    │  ║
║  │  - pm2 start server.js                                   │  ║
║  └──────────────────────────────────────────────────────────┘  ║
║  ┌──────────────────────────────────────────────────────────┐  ║
║  │  PostgreSQL (apt install postgresql)                     │  ║
║  │  - Port 5432                                             │  ║
║  │  - Config: /etc/postgresql/                              │  ║
║  └──────────────────────────────────────────────────────────┘  ║
║                                                                ║
║  📁 Shared: Libraries, Dependencies, System Resources          ║
╚════════════════════════════════════════════════════════════════╝
```

### ❌ ปัญหาที่พบใน VM Deployment

#### ปัญหาที่ 1: Setup ใช้เวลานาน

```bash
# ติดตั้ง PostgreSQL
sudo apt update                           # 2-3 นาที
sudo apt install postgresql postgresql-contrib  # 5 นาที
sudo -u postgres createuser --interactive  # 1 นาที
sudo -u postgres createdb taskboard        # 1 นาที

# ติดตั้ง Node.js
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20                             # 3-5 นาที
npm install -g pm2                          # 1 นาที

# ติดตั้ง Nginx
sudo apt install nginx                      # 2 นาที
sudo nano /etc/nginx/sites-available/app   # 5-10 นาที (config)
sudo systemctl restart nginx                # 1 นาที

# ────────────────────────────────────────────
# รวมเวลา: 30-50 นาที (ถ้าไม่มี error!)
```

#### ปัญหาที่ 2: "Works on my machine!" 😱

```
┌─────────────────┐              ┌─────────────────┐
│    นักศึกษา A     │              │    นักศึกษา B     │
│                 │              │                 │
│  Ubuntu 24.04   │     ≠        │  Ubuntu 22.04   │
│  PostgreSQL 16  │              │  PostgreSQL 14  │
│  Node.js 20.10  │              │  Node.js 18.19  │
│  npm 10.2       │              │  npm 9.5        │
└─────────────────┘              └─────────────────┘
        │                                │
        ▼                                ▼
   ✅ ทำงานได้                        ❌ Error!

สาเหตุ: Version ต่างกัน, Libraries ต่างกัน, Config ต่างกัน
```

#### ปัญหาที่ 3: Dependency Hell

```
┌───────────────────────────────────────────────────────────────┐
│  Your Application                                             │
│  ├── needs: libpq 16.x                                        │
│  └── needs: openssl 3.x                                       │
│                                                               │
│  Another Application (on same VM)                             │
│  ├── needs: libpq 14.x  ← ⚠️ CONFLICT!                        │
│  └── needs: openssl 1.1 ← ⚠️ CONFLICT!                        │
└───────────────────────────────────────────────────────────────┘

ผลลัพธ์: ต้องเลือกว่าจะใช้ version ไหน หรือต้องแยก VM
```

#### ปัญหาที่ 4: Resource Overhead

```
╔═══════════════════════════════════════════════════════════════╗
║  ถ้าต้องการรัน 3 Applications                                    ║
╠═══════════════════════════════════════════════════════════════╣
║                                                               ║
║  แบบ VM Traditional:                                          ║
║  ┌─────────┐ ┌─────────┐ ┌─────────┐                          ║
║  │  VM 1   │ │  VM 2   │ │  VM 3   │                          ║
║  │ Ubuntu  │ │ Ubuntu  │ │ Ubuntu  │  ← OS ซ้ำ 3 ชุด!           ║
║  │ 2GB RAM │ │ 2GB RAM │ │ 2GB RAM │                          ║
║  │ App A   │ │ App B   │ │ App C   │                          ║
║  └─────────┘ └─────────┘ └─────────┘                          ║
║                                                               ║
║  📊 ใช้ทรัพยากร: 6GB RAM + 60GB Disk (OS 20GB x 3)              ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
```

#### ปัญหาที่ 5: Cleanup ยุ่งยาก

```bash
# ลบ PostgreSQL ออกจากระบบ
sudo apt remove postgresql postgresql-contrib  # ลบตัวโปรแกรม
sudo apt purge postgresql*                      # ลบ config
sudo rm -rf /var/lib/postgresql/               # ลบ data
sudo rm -rf /etc/postgresql/                   # ลบ config ที่เหลือ
sudo deluser postgres                           # ลบ user

# แต่ยังอาจมี:
# - Libraries ที่ติดตั้งมา (shared dependencies)
# - Log files ที่กระจายอยู่
# - Environment variables ที่ตั้งไว้
```

### ✅ Docker แก้ปัญหาเหล่านี้ได้!

```
╔═══════════════════════════════════════════════════════════════╗
║              Docker Solution                                  ║
╠═══════════════════════════════════════════════════════════════╣
║                                                               ║
║  1. Setup ในคำสั่งเดียว:                                         ║
║     $ docker compose up -d                                    ║
║     ✅ เสร็จใน < 1 นาที!                                        ║
║                                                               ║
║  2. Environment เหมือนกันทุกคน:                                  ║
║     Same Image = Same Environment = No "works on my machine"  ║
║                                                               ║
║  3. Isolated Containers:                                      ║
║     แต่ละ Container มี dependencies ของตัวเอง ไม่ขัดกัน             ║
║                                                               ║
║  4. Lightweight:                                              ║
║     Containers share Host OS kernel → ใช้ resources น้อยกว่า     ║
║                                                               ║
║  5. Cleanup ง่าย:                                              ║
║     $ docker compose down -v                                  ║
║     ✅ ลบหมดไม่เหลือ traces                                     ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
```

---

## 3. Docker คืออะไร?

### 📖 นิยามของ Docker

> **Docker** คือ Platform สำหรับสร้าง, แจกจ่าย และรัน Applications ใน **Containers**

### 🤔 Container คืออะไร?

**Container** = Package ที่รวมทุกอย่างที่ Application ต้องการ:
- Source Code
- Runtime (Node.js, Python, Java)
- Libraries & Dependencies
- Configuration Files
- Environment Variables

```
╔════════════════════════════════════════════════════════════════╗
║                    Container ประกอบด้วย                         ║
╠════════════════════════════════════════════════════════════════╣
║                                                                ║
║   ┌───────────────────────────────────────────────────────┐    ║
║   │              Your Application Code                    │    ║
║   │              (server.js, *.html, *.css)               │    ║
║   └───────────────────────────────────────────────────────┘    ║
║   ┌───────────────────────────────────────────────────────┐    ║
║   │              Runtime Environment                      │    ║
║   │              (Node.js 20, npm, etc.)                  │    ║
║   └───────────────────────────────────────────────────────┘    ║
║   ┌───────────────────────────────────────────────────────┐    ║
║   │              Dependencies                             │    ║
║   │              (express, pg, cors, etc.)                │    ║
║   └───────────────────────────────────────────────────────┘    ║
║   ┌───────────────────────────────────────────────────────┐    ║
║   │              Configuration                            │    ║
║   │              (env vars, config files)                 │    ║
║   └───────────────────────────────────────────────────────┘    ║
║                                                                ║
║   📦 ทุกอย่างอยู่ใน Package เดียว - ย้ายไปไหนก็รันได้เหมือนกัน!           ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝
```

### 🆚 Container vs Virtual Machine

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Virtual Machine                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                       │
│   │    App A    │ │    App B    │ │    App C    │                       │
│   └─────────────┘ └─────────────┘ └─────────────┘                       │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                       │
│   │   Bins/Libs │ │   Bins/Libs │ │   Bins/Libs │   ← ซ้ำกัน!             │
│   └─────────────┘ └─────────────┘ └─────────────┘                       │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                       │
│   │  Guest OS   │ │  Guest OS   │ │  Guest OS   │   ← OS ซ้ำ 3 ชุด!       │
│   │  (Ubuntu)   │ │  (Ubuntu)   │ │  (Ubuntu)   │   (หนักมาก!)           │
│   └─────────────┘ └─────────────┘ └─────────────┘                       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                    Hypervisor (VMware, VirtualBox)          │       │
│   └─────────────────────────────────────────────────────────────┘       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                         Host OS                             │       │
│   └─────────────────────────────────────────────────────────────┘       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                      Hardware (Server)                      │       │
│   └─────────────────────────────────────────────────────────────┘       │
│                                                                         │
│   📊 RAM: ~6GB | Disk: ~60GB | Boot: 30-60 วินาที                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        Containers (Docker)                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                       │
│   │    App A    │ │    App B    │ │    App C    │                       │
│   └─────────────┘ └─────────────┘ └─────────────┘                       │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                       │
│   │   Bins/Libs │ │   Bins/Libs │ │   Bins/Libs │   ← เฉพาะที่ต้องการ      │
│   └─────────────┘ └─────────────┘ └─────────────┘                       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                    Docker Engine                            │       │
│   └─────────────────────────────────────────────────────────────┘       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                    Host OS (shared!)                        │       │
│   └─────────────────────────────────────────────────────────────┘       │
│   ┌─────────────────────────────────────────────────────────────┐       │
│   │                      Hardware (Server)                      │       │
│   └─────────────────────────────────────────────────────────────┘       │
│                                                                         │
│   📊 RAM: ~600MB | Disk: ~3GB | Boot: 1-5 วินาที                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📊 ตารางเปรียบเทียบ VM vs Container

| คุณสมบัติ | Virtual Machine | Container |
|---------|-----------------|-----------|
| **Boot Time** | 30-60 วินาที | 1-5 วินาที |
| **Size** | GB ต่อ VM | MB ต่อ Container |
| **RAM Usage** | ต้องจองล่วงหน้า | ใช้เท่าที่ต้องการ |
| **OS** | แต่ละ VM มี OS ของตัวเอง | Share Host OS Kernel |
| **Isolation** | Complete (Hardware level) | Process level |
| **Portability** | ต้องใช้ Hypervisor เดียวกัน | รันได้ทุกที่ที่มี Docker |
| **Performance** | 70-80% ของ bare metal | 95-99% ของ bare metal |

---

## 4. Docker Concepts พื้นฐาน

### 4.1 Docker Architecture

```
╔════════════════════════════════════════════════════════════════════════╗
║                        Docker Architecture                             ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║   Client (CLI)                 Docker Host                             ║
║   ┌──────────────┐             ┌────────────────────────────────────┐  ║
║   │              │  REST API   │                                    │  ║
║   │ docker build ├────────────►│  Docker Daemon (dockerd)           │  ║
║   │ docker pull  │             │  ┌──────────────────────────────┐  │  ║
║   │ docker run   │             │  │  Containers                  │  │  ║
║   │ docker push  │             │  │  ┌─────┐ ┌─────┐ ┌─────┐     │  │  ║
║   │              │             │  │  │ C1  │ │ C2  │ │ C3  │     │  │  ║
║   └──────────────┘             │  │  └─────┘ └─────┘ └─────┘     │  │  ║
║                                │  └──────────────────────────────┘  │  ║
║                                │  ┌──────────────────────────────┐  │  ║
║                                │  │  Images                      │  │  ║
║                                │  │  [nginx] [node] [postgres]   │  │  ║
║                                │  └──────────────────────────────┘  │  ║
║                                └────────────────────────────────────┘  ║
║                                             │                          ║
║                                             │ pull/push                ║
║                                             ▼                          ║
║                                ┌────────────────────────────────────┐  ║
║                                │        Docker Registry             │  ║
║                                │        (Docker Hub)                │  ║
║                                │  ┌───────────────────────────────┐ │  ║
║                                │  │ nginx:latest                  │ │  ║
║                                │  │ node:20-alpine                │ │  ║
║                                │  │ postgres:16-alpine            │ │  ║
║                                │  │ your-username/your-image      │ │  ║
║                                │  └───────────────────────────────┘ │  ║
║                                └────────────────────────────────────┘  ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 4.2 Docker Image

**Image** = Template/Blueprint สำหรับสร้าง Container (เหมือน Class ใน OOP)

```
┌────────────────────────────────────────────────────────────────────────┐
│                          Docker Image                                  │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  คุณสมบัติ:                                                               │
│  • Read-only (ไม่เปลี่ยนแปลงได้)                                           │
│  • ประกอบด้วยหลาย Layers (ซ้อนกัน)                                        │
│  • สามารถ Share และ Reuse layers ได้                                    │
│  • มี Tag สำหรับระบุ version (nginx:1.25, nginx:latest)                   │
│                                                                        │
│  Image Layers:                                                         │
│  ┌────────────────────────────────────────┐                            │
│  │  Layer 4: Application Code (your code) │  ← เปลี่ยนบ่อย                │
│  ├────────────────────────────────────────┤                            │
│  │  Layer 3: npm install (dependencies)   │                            │
│  ├────────────────────────────────────────┤                            │
│  │  Layer 2: Node.js Runtime              │                            │
│  ├────────────────────────────────────────┤                            │
│  │  Layer 1: Alpine Linux (Base OS)       │  ← เปลี่ยนน้อยมาก             │
│  └────────────────────────────────────────┘                            │
│                                                                        │
│  💡 ถ้าแก้แค่ code → rebuild เฉพาะ Layer 4 (เร็วมาก!)                      │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 4.3 Docker Container

**Container** = Running instance ของ Image (เหมือน Object ที่สร้างจาก Class)

```
┌────────────────────────────────────────────────────────────────────────┐
│                         Docker Container                               │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ความสัมพันธ์:                                                            │
│                                                                        │
│  ┌─────────────┐      docker run       ┌─────────────┐                 │
│  │   IMAGE     │ ───────────────────►  │  CONTAINER  │                 │
│  │  (Template) │                       │  (Running)  │                 │
│  └─────────────┘                       └─────────────┘                 │
│                                                                        │
│  1 Image สามารถสร้างได้หลาย Containers:                                  │
│                                                                        │
│  ┌─────────────┐      ┌────────────┐                                   │
│  │   nginx     │ ───► │ web-1      │ (Container 1)                     │
│  │   :alpine   │ ───► │ web-2      │ (Container 2)                     │
│  │   (Image)   │ ───► │ web-3      │ (Container 3)                     │
│  └─────────────┘      └────────────┘                                   │
│                                                                        │
│  คุณสมบัติ:                                                               │
│  • มี Writable layer ด้านบน (เปลี่ยนแปลงได้)                                │
│  • แต่ละ Container แยก isolated                                         │
│  • สามารถ start/stop/restart ได้                                        │
│  • ข้อมูลจะหายเมื่อลบ Container (ถ้าไม่ใช้ Volume)                            │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 4.4 Dockerfile

**Dockerfile** = Script สำหรับสร้าง Image (เหมือน Recipe)

```dockerfile
# ตัวอย่าง Dockerfile สำหรับ Node.js Application

# Layer 1: Base Image
FROM node:20-alpine

# Layer 2: Set Working Directory
WORKDIR /app

# Layer 3: Copy package files (cache optimization)
COPY package*.json ./

# Layer 4: Install dependencies
RUN npm install

# Layer 5: Copy source code
COPY . .

# Layer 6: Expose port
EXPOSE 3000

# Layer 7: Start command
CMD ["node", "server.js"]
```

### 4.5 Docker Network

Docker สร้าง Network ให้ Containers คุยกันได้:

```
╔════════════════════════════════════════════════════════════════════════╗
║                        Docker Network                                  ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  ┌────────────────────────────────────────────────────────────────┐    ║
║  │              Docker Network: my-app-network                    │    ║
║  │                                                                │    ║
║  │   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐      │    ║
║  │   │   nginx     │     │    api      │     │     db      │      │    ║
║  │   │ Container   │ ──► │ Container   │ ──► │ Container   │      │    ║
║  │   │             │     │             │     │             │      │    ║
║  │   │  nginx:80   │     │  api:3000   │     │  db:5432    │      │    ║
║  │   └─────────────┘     └─────────────┘     └─────────────┘      │    ║
║  │                                                                │    ║
║  │   💡 ใช้ชื่อ Container เป็น hostname ได้เลย!                        │    ║
║  │      nginx ติดต่อ api ได้ที่ http://api:3000                        │    ║
║  │      api ติดต่อ db ได้ที่ db:5432                                   │    ║
║  │                                                                │    ║
║  └────────────────────────────────────────────────────────────────┘    ║
║                                                                        ║
║  Network Types:                                                        ║
║  • bridge  - Default, containers คุยกันผ่าน Docker DNS                    ║
║  • host    - ใช้ network ของ host โดยตรง                                ║
║  • none    - ไม่มี network                                               ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 4.6 Docker Volume

**Volume** = พื้นที่เก็บข้อมูลแบบ Persistent (ไม่หายเมื่อลบ Container)

```
╔════════════════════════════════════════════════════════════════════════╗
║                         Docker Volumes                                 ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  ปัญหา: ข้อมูลใน Container จะหายเมื่อลบ Container                           ║
║                                                                        ║
║  ❌ Without Volume:                                                    ║
║  ┌───────────────┐                                                     ║
║  │  PostgreSQL   │                                                     ║
║  │  Container    │  ──► docker rm db ──► 💥 Data Lost!                 ║
║  │  [data]       │                                                     ║
║  └───────────────┘                                                     ║
║                                                                        ║
║  ✅ With Volume:                                                       ║
║  ┌───────────────┐       ┌───────────────────────────────┐             ║
║  │  PostgreSQL   │       │  Volume: postgres_data        │             ║
║  │  Container    │ ◄───► │  (on Host filesystem)         │             ║
║  │               │       │  [data persists here]         │             ║
║  └───────────────┘       └───────────────────────────────┘             ║
║         │                                                              ║
║         ▼                                                              ║
║  docker rm db ──► Container ถูกลบ                                       ║
║                   แต่ data ยังอยู่ใน Volume!                               ║
║         │                                                              ║
║         ▼                                                              ║
║  docker run db ──► Container ใหม่ mount volume เดิม                      ║
║                   ✅ Data ยังอยู่ครบ!                                     ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 4.7 สรุป Docker Components

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Docker Components Summary                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Component      │  คืออะไร                    │  เปรียบเทียบ               │
│  ──────────────────────────────────────────────────────────────────     │
│  Dockerfile     │  Script สร้าง Image         │  Recipe / พิมพ์เขียว        │
│  Image          │  Template/Blueprint        │  Class ใน OOP            │
│  Container      │  Running instance          │  Object ใน OOP           │
│  Registry       │  Storage สำหรับ Images      │  npm registry            │
│  Network        │  การสื่อสารระหว่าง Containers │  LAN/VLAN                │
│  Volume         │  Persistent storage        │  External HDD            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. ติดตั้งและตรวจสอบ Docker

### 5.1 การติดตั้ง Docker

#### Windows 11 + WSL2

```bash
# 1. ติดตั้ง Docker Desktop จาก https://docker.com

# 2. เปิดใช้ WSL2 Integration ใน Docker Desktop Settings
#    Settings > Resources > WSL Integration > Enable Ubuntu

# 3. ตรวจสอบใน WSL Terminal
wsl
docker --version
```

#### macOS (Apple Silicon M2)

```bash
# 1. ติดตั้ง Docker Desktop for Mac (Apple Silicon)
#    Download จาก https://docker.com

# 2. ตรวจสอบ
docker --version
```

#### Ubuntu Linux

```bash
# 1. Update และติดตั้ง dependencies
sudo apt update
sudo apt install ca-certificates curl gnupg

# 2. เพิ่ม Docker's official GPG key
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

# 3. เพิ่ม Docker repository
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 4. ติดตั้ง Docker
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 5. เพิ่ม user ปัจจุบันเข้ากลุ่ม docker (ไม่ต้องใช้ sudo)
sudo usermod -aG docker $USER

# 6. Restart หรือ logout/login
```

### 5.2 ตรวจสอบการติดตั้ง

```bash
# ตรวจสอบ Docker version
docker --version
# ผลลัพธ์: Docker version 24.0.x, build xxxxxx

# ตรวจสอบ Docker Compose version
docker compose version
# ผลลัพธ์: Docker Compose version v2.x.x

# ตรวจสอบ Docker Engine ทำงาน
docker info
# ผลลัพธ์: ข้อมูล Docker system

# ทดสอบ Hello World
docker run hello-world
# ผลลัพธ์: Hello from Docker! ...
```

### 🎯 Demo: ทดสอบ Docker Hello World

```bash
# รัน hello-world container
$ docker run hello-world

# ผลลัพธ์:
Unable to find image 'hello-world:latest' locally
latest: Pulling from library/hello-world
c1ec31eb5944: Pull complete 
Digest: sha256:...
Status: Downloaded newer image for hello-world:latest

Hello from Docker!
This message shows that your installation appears to be working correctly.

To generate this message, Docker took the following steps:
 1. The Docker client contacted the Docker daemon.
 2. The Docker daemon pulled the "hello-world" image from the Docker Hub.
 3. The Docker daemon created a new container from that image.
 4. The Docker daemon streamed that output to the Docker client.
 ...
```

**เกิดอะไรขึ้นบ้าง?**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                  docker run hello-world                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Step 1: Docker client ส่งคำสั่งไปยัง Docker daemon                         │
│          ┌──────────┐        ┌──────────────┐                           │
│          │  docker  │ ─────► │   dockerd    │                           │
│          │  (CLI)   │        │  (daemon)    │                           │
│          └──────────┘        └──────────────┘                           │
│                                                                         │
│  Step 2: Docker daemon ค้นหา hello-world image ในเครื่อง                   │
│          ❌ ไม่พบ → ไป pull จาก Docker Hub                               │
│                                                                         │
│  Step 3: Pull image จาก Docker Hub                                      │
│          ┌──────────────┐       ┌─────────────────┐                     │
│          │   dockerd    │ ◄──── │   Docker Hub    │                     │
│          └──────────────┘       │ hello-world:    │                     │
│                                 │   latest        │                     │
│                                 └─────────────────┘                     │
│                                                                         │
│  Step 4: สร้าง container จาก image และรัน                                 │
│          ┌──────────────┐                                               │
│          │  Container   │ ──► Print "Hello from Docker!" ──► Exit       │
│          └──────────────┘                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 6. Docker Hub และ Registry

### 6.1 Docker Hub คืออะไร?

> **Docker Hub** = Public registry สำหรับเก็บและแชร์ Docker Images
> เปรียบเทียบได้กับ GitHub สำหรับ code หรือ npm registry สำหรับ packages

**URL:** https://hub.docker.com

### 6.2 สมัครบัญชี Docker Hub

1. ไปที่ https://hub.docker.com
2. คลิก "Sign Up"
3. กรอกข้อมูล:
   - Username (จะใช้เป็น namespace: username/image-name)
   - Email
   - Password
4. Verify email
5. Login สำเร็จ

### 6.3 Login Docker Hub จาก CLI

```bash
# Login
$ docker login
Username: your-username
Password: ********
Login Succeeded

# ตรวจสอบการ login
$ docker info | grep Username
Username: your-username

# Logout (เมื่อต้องการ)
$ docker logout
```

### 6.4 ประเภทของ Images บน Docker Hub

```
╔════════════════════════════════════════════════════════════════════════╗
║                    Types of Docker Images                              ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  1. Official Images (ไม่มี prefix)                                       ║
║     ┌─────────────────────────────────────────────────────────────┐    ║
║     │  nginx          - Web server                                │    ║
║     │  node           - Node.js runtime                           │    ║
║     │  postgres       - PostgreSQL database                       │    ║
║     │  redis          - In-memory cache                           │    ║
║     │  mysql          - MySQL database                            │    ║
║     │  python         - Python runtime                            │    ║
║     │  ubuntu         - Ubuntu OS base                            │    ║
║     │  alpine         - Minimal Linux base (5MB!)                 │    ║
║     └─────────────────────────────────────────────────────────────┘    ║
║     ✅ ดูแลโดย Docker หรือ Project maintainers                           ║
║     ✅ มีการ scan security และ update สม่ำเสมอ                           ║
║                                                                        ║
║  2. Verified Publisher Images (prefix: publisher/)                     ║
║     ┌─────────────────────────────────────────────────────────────┐    ║
║     │  bitnami/postgresql                                         │    ║
║     │  grafana/grafana                                            │    ║
║     │  hashicorp/consul                                           │    ║
║     └─────────────────────────────────────────────────────────────┘    ║
║     ✅ ดูแลโดยบริษัท/องค์กรที่ได้รับการยืนยัน                                    ║
║                                                                        ║
║  3. User Images (prefix: username/)                                    ║
║     ┌─────────────────────────────────────────────────────────────┐    ║
║     │  your-username/my-app                                       │    ║
║     │  somchai/thai-api                                           │    ║
║     └─────────────────────────────────────────────────────────────┘    ║
║     ⚠️ สร้างโดย users ทั่วไป - ควรตรวจสอบก่อนใช้                            ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 6.5 Image Tags

**Tag** = version/variant ของ Image

```bash
# รูปแบบ: image-name:tag
nginx:latest           # Latest version (default)
nginx:1.25             # Version 1.25
nginx:1.25-alpine      # Version 1.25 บน Alpine Linux (เล็กกว่า)
nginx:mainline         # Development version

node:20                # Node.js 20 (Debian-based)
node:20-alpine         # Node.js 20 บน Alpine (เล็กกว่ามาก!)
node:20-slim           # Node.js 20 slim version

postgres:16            # PostgreSQL 16
postgres:16-alpine     # PostgreSQL 16 บน Alpine
postgres:15            # PostgreSQL 15 (older version)
```

**📊 เปรียบเทียบขนาด Image:**

```
┌────────────────────────────────────────────────────────────────┐
│  Image                   │  Size      │  Use Case              │
├────────────────────────────────────────────────────────────────┤
│  node:20                 │  ~1.1 GB   │  Full features         │
│  node:20-slim            │  ~240 MB   │  Reduced size          │
│  node:20-alpine          │  ~140 MB   │  Minimal (recommend)   │
├────────────────────────────────────────────────────────────────┤
│  postgres:16             │  ~430 MB   │  Full features         │
│  postgres:16-alpine      │  ~240 MB   │  Minimal (recommend)   │
├────────────────────────────────────────────────────────────────┤
│  nginx:latest            │  ~190 MB   │  Full features         │
│  nginx:alpine            │  ~40 MB    │  Minimal (recommend)   │
└────────────────────────────────────────────────────────────────┘

💡 Tip: ใช้ Alpine variants เพื่อลดขนาด image และ security surface
```

### 6.6 ค้นหา Images บน Docker Hub

```bash
# ค้นหา images
$ docker search nginx

NAME                      DESCRIPTION                     STARS   OFFICIAL
nginx                     Official build of Nginx.        19000   [OK]
bitnami/nginx             Bitnami nginx Docker Image      180
nginx/nginx-prometheus..  NGINX Prometheus Exporter       30
...

# หรือค้นหาบน https://hub.docker.com
```

### 🎯 Demo: Pull และดู Images

```bash
# Pull nginx image
$ docker pull nginx:alpine
alpine: Pulling from library/nginx
c6a83fedfae6: Pull complete 
b9...
Digest: sha256:...
Status: Downloaded newer image for nginx:alpine
docker.io/library/nginx:alpine

# ดู images ทั้งหมดในเครื่อง
$ docker images
REPOSITORY    TAG       IMAGE ID       CREATED        SIZE
nginx         alpine    a8758716bb6a   2 weeks ago    43.2MB
hello-world   latest    d2c94e258dcb   8 months ago   13.3kB

# ดูรายละเอียด image
$ docker inspect nginx:alpine
[
    {
        "Id": "sha256:...",
        "RepoTags": ["nginx:alpine"],
        "Architecture": "amd64",
        ...
    }
]
```

---

## 7. การใช้งาน Docker Commands พื้นฐาน

### 7.1 Image Commands

```bash
# ══════════════════════════════════════════════════════════════
# IMAGE MANAGEMENT COMMANDS
# ══════════════════════════════════════════════════════════════

# Pull image จาก Docker Hub
docker pull <image>:<tag>
docker pull nginx:alpine
docker pull node:20-alpine
docker pull postgres:16-alpine

# ดู images ทั้งหมดในเครื่อง
docker images
docker image ls

# ลบ image
docker rmi <image-name>
docker image rm nginx:alpine

# ลบ images ที่ไม่ได้ใช้ (dangling)
docker image prune

# ดูประวัติการสร้าง image (layers)
docker history nginx:alpine
```

### 7.2 Container Commands

```bash
# ══════════════════════════════════════════════════════════════
# CONTAINER LIFECYCLE COMMANDS  
# ══════════════════════════════════════════════════════════════

# รัน container
docker run <image>                    # รันและ attach terminal
docker run -d <image>                 # รันแบบ detached (background)
docker run -d --name my-nginx nginx   # ตั้งชื่อ container

# ดู containers ที่กำลังรัน
docker ps

# ดู containers ทั้งหมด (รวมที่หยุดแล้ว)
docker ps -a

# หยุด container
docker stop <container-name-or-id>
docker stop my-nginx

# Start container ที่หยุดแล้ว
docker start my-nginx

# Restart container
docker restart my-nginx

# ลบ container
docker rm <container-name-or-id>      # ต้อง stop ก่อน
docker rm -f <container-name-or-id>   # force remove (หยุดและลบ)

# ลบ containers ที่หยุดแล้วทั้งหมด
docker container prune
```

### 7.3 Port Mapping

```bash
# ══════════════════════════════════════════════════════════════
# PORT MAPPING (-p host:container)
# ══════════════════════════════════════════════════════════════

# Map port 8080 บน host ไปยัง port 80 ใน container
docker run -d -p 8080:80 --name web nginx:alpine

# เข้าถึงได้ที่ http://localhost:8080

# Map หลาย ports
docker run -d -p 80:80 -p 443:443 nginx

# ดู port mappings
docker port web
# 80/tcp -> 0.0.0.0:8080
```

```
┌─────────────────────────────────────────────────────────────────┐
│  Port Mapping: -p 8080:80                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Browser                     Docker Host                        │
│  ┌──────────┐               ┌──────────────────────────────┐    │
│  │          │               │                              │    │
│  │ localhost├───────────────►  Port 8080                   │    │
│  │ :8080    │               │      │                       │    │
│  │          │               │      │ maps to               │    │
│  └──────────┘               │      ▼                       │    │
│                             │  ┌────────────────────────┐  │    │
│                             │  │  nginx container       │  │    │
│                             │  │  ┌──────────────────┐  │  │    │
│                             │  │  │  Port 80 (nginx) │  │  │    │
│                             │  │  └──────────────────┘  │  │    │
│                             │  └────────────────────────┘  │    │
│                             └──────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.4 Volume Mounting

```bash
# ══════════════════════════════════════════════════════════════
# VOLUME COMMANDS
# ══════════════════════════════════════════════════════════════

# สร้าง named volume
docker volume create my-data

# Mount volume เข้า container
docker run -d -v my-data:/app/data my-app

# Bind mount (folder บน host)
docker run -d -v $(pwd)/html:/usr/share/nginx/html nginx

# ดู volumes
docker volume ls

# ลบ volume
docker volume rm my-data

# ลบ volumes ที่ไม่ได้ใช้
docker volume prune
```

### 7.5 Environment Variables

```bash
# ══════════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES
# ══════════════════════════════════════════════════════════════

# ตั้ง environment variable
docker run -e "NODE_ENV=production" node:alpine

# หลาย variables
docker run -e "DB_HOST=localhost" -e "DB_PORT=5432" my-app

# ใช้ไฟล์ .env
docker run --env-file .env my-app
```

### 7.6 ดู Logs และ Execute Commands

```bash
# ══════════════════════════════════════════════════════════════
# LOGS & EXEC
# ══════════════════════════════════════════════════════════════

# ดู logs ของ container
docker logs my-nginx
docker logs -f my-nginx     # follow (real-time)
docker logs --tail 50 my-nginx  # 50 บรรทัดสุดท้าย

# รัน command ใน container ที่กำลังทำงาน
docker exec my-nginx ls -la
docker exec -it my-nginx sh     # interactive shell
docker exec -it my-nginx bash   # bash shell

# -it = interactive + tty (สำหรับ shell)
```

### 🎯 Demo: รัน Nginx Container

```bash
# ══════════════════════════════════════════════════════════════
# DEMO: Running Nginx Web Server
# ══════════════════════════════════════════════════════════════

# Step 1: Pull nginx image
$ docker pull nginx:alpine

# Step 2: รัน nginx container
$ docker run -d --name my-web -p 8080:80 nginx:alpine

# ตรวจสอบ container
$ docker ps
CONTAINER ID   IMAGE          COMMAND                  CREATED          STATUS          PORTS                  NAMES
a1b2c3d4e5f6   nginx:alpine   "/docker-entrypoint.…"   10 seconds ago   Up 9 seconds    0.0.0.0:8080->80/tcp   my-web

# Step 3: เปิด browser ไปที่ http://localhost:8080
# จะเห็นหน้า Welcome to nginx!

# Step 4: ดู logs
$ docker logs my-web
...
2024/xx/xx xx:xx:xx [notice] 1#1: start worker processes
...

# Step 5: เข้าไปใน container
$ docker exec -it my-web sh
/ # ls
bin    etc    lib    mnt    proc   run    srv    tmp    var
dev    home   media  opt    root   sbin   sys    usr
/ # cat /etc/nginx/nginx.conf
... (nginx configuration)
/ # exit

# Step 6: หยุดและลบ container
$ docker stop my-web
$ docker rm my-web
```

### 🎯 Demo: รัน PostgreSQL Container

```bash
# ══════════════════════════════════════════════════════════════
# DEMO: Running PostgreSQL Database
# ══════════════════════════════════════════════════════════════

# Step 1: รัน PostgreSQL พร้อม environment variables และ volume
$ docker run -d \
    --name my-db \
    -e POSTGRES_USER=admin \
    -e POSTGRES_PASSWORD=secret123 \
    -e POSTGRES_DB=myapp \
    -v pgdata:/var/lib/postgresql/data \
    -p 5432:5432 \
    postgres:16-alpine

# ตรวจสอบ
$ docker ps
CONTAINER ID   IMAGE                PORTS                    NAMES
xyz123         postgres:16-alpine   0.0.0.0:5432->5432/tcp   my-db

# Step 2: เชื่อมต่อ database
$ docker exec -it my-db psql -U admin -d myapp

myapp=# CREATE TABLE users (id SERIAL PRIMARY KEY, name VARCHAR(100));
CREATE TABLE

myapp=# INSERT INTO users (name) VALUES ('สมชาย');
INSERT 0 1

myapp=# SELECT * FROM users;
 id | name  
----+-------
  1 | สมชาย
(1 row)

myapp=# \q

# Step 3: ลบ container (data ยังอยู่ใน volume)
$ docker stop my-db
$ docker rm my-db

# Step 4: สร้าง container ใหม่ mount volume เดิม
$ docker run -d \
    --name my-db \
    -e POSTGRES_USER=admin \
    -e POSTGRES_PASSWORD=secret123 \
    -e POSTGRES_DB=myapp \
    -v pgdata:/var/lib/postgresql/data \
    -p 5432:5432 \
    postgres:16-alpine

# ตรวจสอบ data ยังอยู่
$ docker exec -it my-db psql -U admin -d myapp -c "SELECT * FROM users;"
 id | name  
----+-------
  1 | สมชาย
(1 row)

# ✅ Data persist ผ่าน volume!
```

### 🎯 Demo: รัน Node.js Application

```bash
# ══════════════════════════════════════════════════════════════
# DEMO: Running Node.js Application
# ══════════════════════════════════════════════════════════════

# สร้าง project folder
$ mkdir demo-node && cd demo-node

# สร้างไฟล์ server.js
$ cat > server.js << 'EOF'
const http = require('http');

const server = http.createServer((req, res) => {
  res.writeHead(200, { 'Content-Type': 'text/html; charset=utf-8' });
  res.end('<h1>สวัสดี Docker! 🐳</h1><p>Running on Node.js ' + process.version + '</p>');
});

server.listen(3000, () => {
  console.log('Server running at http://localhost:3000/');
});
EOF

# รัน Node.js แบบ quick test (bind mount code)
$ docker run -d \
    --name my-node \
    -p 3000:3000 \
    -v $(pwd):/app \
    -w /app \
    node:20-alpine \
    node server.js

# เปิด browser ไปที่ http://localhost:3000
# จะเห็น "สวัสดี Docker! 🐳"

# ดู logs
$ docker logs my-node
Server running at http://localhost:3000/

# Cleanup
$ docker stop my-node && docker rm my-node
```

---

## 8. สร้าง Dockerfile แรกของคุณ

### 8.1 โครงสร้าง Dockerfile

```dockerfile
# ══════════════════════════════════════════════════════════════
# DOCKERFILE STRUCTURE
# ══════════════════════════════════════════════════════════════

# 1. Base Image - เริ่มจาก image ที่มีอยู่
FROM node:20-alpine

# 2. Metadata (optional)
LABEL maintainer="your-email@example.com"
LABEL version="1.0"

# 3. Set Working Directory
WORKDIR /app

# 4. Copy dependency files (สำหรับ caching)
COPY package*.json ./

# 5. Install dependencies
RUN npm install

# 6. Copy source code
COPY . .

# 7. Expose port
EXPOSE 3000

# 8. Environment Variables (optional)
ENV NODE_ENV=production

# 9. Start command
CMD ["node", "server.js"]
```

### 8.2 Dockerfile Instructions อธิบาย

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DOCKERFILE INSTRUCTIONS                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Instruction     │  คำอธิบาย                                             │
│  ──────────────────────────────────────────────────────────────────     │
│  FROM            │  Base image ที่จะใช้                                    │
│  WORKDIR         │  Set working directory ใน container                  │
│  COPY            │  Copy files จาก host เข้า container                   │
│  ADD             │  เหมือน COPY แต่รองรับ URL และ auto-extract tar         │
│  RUN             │  รัน command ตอน build (สร้าง layer ใหม่)               │
│  CMD             │  Default command เมื่อ container start                 │
│  ENTRYPOINT      │  Executable ที่รันเสมอ (CMD เป็น arguments)              │
│  EXPOSE          │  Document port ที่ container ใช้                        │
│  ENV             │  Set environment variable                            │
│  ARG             │  Build-time variable                                 │
│  VOLUME          │  Create mount point                                  │
│  USER            │  Set user สำหรับ RUN, CMD, ENTRYPOINT                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.3 Build และ Run Custom Image

```bash
# ══════════════════════════════════════════════════════════════
# BUILD & RUN CUSTOM IMAGE
# ══════════════════════════════════════════════════════════════

# Build image จาก Dockerfile
docker build -t my-app:1.0 .

# อธิบาย:
# -t my-app:1.0  = ตั้งชื่อและ tag
# .              = build context (current directory)

# ดู image ที่สร้าง
docker images
REPOSITORY   TAG   IMAGE ID       SIZE
my-app       1.0   abc123...      150MB

# รัน container จาก image ที่สร้าง
docker run -d -p 3000:3000 --name app my-app:1.0

# เปิด http://localhost:3000
```

### 🎯 Demo: สร้าง Node.js API Image

```bash
# ══════════════════════════════════════════════════════════════
# DEMO: Build Node.js Express API Image
# ══════════════════════════════════════════════════════════════

# สร้าง project
$ mkdir demo-api && cd demo-api

# สร้าง package.json
$ cat > package.json << 'EOF'
{
  "name": "demo-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
EOF

# สร้าง server.js
$ cat > server.js << 'EOF'
const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.json({ 
    message: 'สวัสดีจาก Docker API! 🐳',
    version: '1.0.0',
    timestamp: new Date().toISOString()
  });
});

app.get('/health', (req, res) => {
  res.json({ status: 'healthy' });
});

app.listen(PORT, () => {
  console.log(`API running on port ${PORT}`);
});
EOF

# สร้าง Dockerfile
$ cat > Dockerfile << 'EOF'
# Base image
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --production

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=3s \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# Start application
CMD ["npm", "start"]
EOF

# สร้าง .dockerignore
$ cat > .dockerignore << 'EOF'
node_modules
npm-debug.log
.git
.gitignore
README.md
EOF

# Build image
$ docker build -t demo-api:1.0 .

# Output:
# [+] Building 15.2s (10/10) FINISHED
#  => [1/5] FROM node:20-alpine
#  => [2/5] WORKDIR /app
#  => [3/5] COPY package*.json ./
#  => [4/5] RUN npm install --production
#  => [5/5] COPY . .
#  => exporting to image

# ดู image
$ docker images demo-api
REPOSITORY   TAG   IMAGE ID       SIZE
demo-api     1.0   abc123...      145MB

# รัน container
$ docker run -d -p 3000:3000 --name api demo-api:1.0

# ทดสอบ API
$ curl http://localhost:3000
{"message":"สวัสดีจาก Docker API! 🐳","version":"1.0.0","timestamp":"2024-..."}

$ curl http://localhost:3000/health
{"status":"healthy"}

# ตรวจสอบ health status
$ docker ps
CONTAINER ID   IMAGE         STATUS                   NAMES
abc123         demo-api:1.0  Up 30 seconds (healthy)  api

# Cleanup
$ docker stop api && docker rm api
```

### 8.4 Push Image ไป Docker Hub

```bash
# ══════════════════════════════════════════════════════════════
# PUSH IMAGE TO DOCKER HUB
# ══════════════════════════════════════════════════════════════

# 1. Login Docker Hub
$ docker login
Username: your-username
Password: ********
Login Succeeded

# 2. Tag image ด้วย username
$ docker tag demo-api:1.0 your-username/demo-api:1.0

# ดู tags
$ docker images
REPOSITORY              TAG   IMAGE ID       SIZE
demo-api                1.0   abc123         145MB
your-username/demo-api  1.0   abc123         145MB  # same image, different tag

# 3. Push ไป Docker Hub
$ docker push your-username/demo-api:1.0
The push refers to repository [docker.io/your-username/demo-api]
abc123: Pushed 
def456: Pushed 
...
1.0: digest: sha256:... size: 1234

# 4. ตรวจสอบบน Docker Hub
# เปิด https://hub.docker.com/r/your-username/demo-api

# 5. ทดสอบ pull จากเครื่องอื่น (หรือลบ local แล้ว pull ใหม่)
$ docker rmi your-username/demo-api:1.0
$ docker pull your-username/demo-api:1.0
$ docker run -d -p 3000:3000 your-username/demo-api:1.0
```

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Push to Docker Hub Flow                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Local Machine                              Docker Hub                  │
│  ┌────────────────────┐                    ┌────────────────────┐       │
│  │                    │                    │                    │       │
│  │  demo-api:1.0      │                    │ your-username/     │       │
│  │  (local image)     │                    │   demo-api:1.0     │       │
│  │                    │                    │                    │       │
│  └─────────┬──────────┘                    └────────────────────┘       │
│            │                                        ▲                   │
│            │  docker tag                            │                   │
│            ▼                                        │                   │
│  ┌────────────────────┐                             │                   │
│  │  your-username/    │      docker push            │                   │
│  │  demo-api:1.0      │ ────────────────────────────┘                   │
│  │  (tagged image)    │                                                 │
│  └────────────────────┘                                                 │
│                                                                         │
│  💡 Image naming: [registry/]username/image-name:tag                    │
│     Examples:                                                           │
│     - nginx:latest           (official, no username)                    │
│     - your-username/my-app:1.0  (your image)                            │
│     - gcr.io/project/app:v1  (Google Container Registry)                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 9. Docker Compose: จัดการหลาย Containers

### 9.1 Docker Compose คืออะไร?

> **Docker Compose** = เครื่องมือสำหรับ define และ run multi-container applications
> ใช้ไฟล์ `docker-compose.yml` แทนการพิมพ์คำสั่ง docker run ยาวๆ

### 9.2 โครงสร้าง docker-compose.yml

```yaml
# docker-compose.yml

# Version (optional ใน Compose v2)
version: '3.8'

# Services (containers)
services:
  # Service 1: Web server
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html
    depends_on:
      - api
    
  # Service 2: API server  
  api:
    build: ./api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_HOST=db
    depends_on:
      - db
      
  # Service 3: Database
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: myapp
    volumes:
      - pgdata:/var/lib/postgresql/data

# Volumes
volumes:
  pgdata:

# Networks (optional - docker compose สร้างให้อัตโนมัติ)
networks:
  default:
    driver: bridge
```

### 9.3 Docker Compose Commands

```bash
# ══════════════════════════════════════════════════════════════
# DOCKER COMPOSE COMMANDS
# ══════════════════════════════════════════════════════════════

# Start all services
docker compose up              # foreground
docker compose up -d           # detached (background)

# Stop all services
docker compose down            # stop และลบ containers
docker compose down -v         # stop และลบ containers + volumes

# Build images
docker compose build           # build all services
docker compose build api       # build specific service

# View status
docker compose ps              # list containers
docker compose logs            # view logs
docker compose logs -f api     # follow logs of specific service

# Execute commands
docker compose exec api sh     # shell into container

# Scale services
docker compose up -d --scale api=3  # รัน 3 instances

# Restart services
docker compose restart
docker compose restart api     # restart specific service
```

### 🎯 Demo: Full Stack with Docker Compose

```bash
# ══════════════════════════════════════════════════════════════
# DEMO: Full Stack Application (Nginx + Node.js + PostgreSQL)
# ══════════════════════════════════════════════════════════════

# สร้าง project structure
$ mkdir fullstack-demo && cd fullstack-demo
$ mkdir -p api frontend

# สร้าง api/package.json
$ cat > api/package.json << 'EOF'
{
  "name": "fullstack-api",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
EOF

# สร้าง api/server.js
$ cat > api/server.js << 'EOF'
const express = require('express');
const { Pool } = require('pg');

const app = express();
const pool = new Pool({
  host: process.env.DB_HOST || 'db',
  user: process.env.DB_USER || 'admin',
  password: process.env.DB_PASSWORD || 'secret',
  database: process.env.DB_NAME || 'myapp',
  port: 5432
});

app.get('/api/health', (req, res) => {
  res.json({ status: 'healthy', service: 'api' });
});

app.get('/api/users', async (req, res) => {
  try {
    const result = await pool.query('SELECT * FROM users');
    res.json(result.rows);
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`API running on port ${PORT}`);
});
EOF

# สร้าง api/Dockerfile
$ cat > api/Dockerfile << 'EOF'
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
EOF

# สร้าง frontend/index.html
$ cat > frontend/index.html << 'EOF'
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>Docker Demo</title>
  <style>
    body { font-family: sans-serif; padding: 20px; background: #1a1a2e; color: #eee; }
    h1 { color: #0db9d7; }
    .card { background: #16213e; padding: 20px; border-radius: 8px; margin: 10px 0; }
  </style>
</head>
<body>
  <h1>🐳 Docker Compose Demo</h1>
  <div class="card">
    <h3>API Health</h3>
    <pre id="health">Loading...</pre>
  </div>
  <div class="card">
    <h3>Users from Database</h3>
    <pre id="users">Loading...</pre>
  </div>
  <script>
    fetch('/api/health')
      .then(r => r.json())
      .then(d => document.getElementById('health').textContent = JSON.stringify(d, null, 2));
    fetch('/api/users')
      .then(r => r.json())
      .then(d => document.getElementById('users').textContent = JSON.stringify(d, null, 2))
      .catch(e => document.getElementById('users').textContent = 'Error: ' + e.message);
  </script>
</body>
</html>
EOF

# สร้าง database/init.sql
$ mkdir database
$ cat > database/init.sql << 'EOF'
CREATE TABLE IF NOT EXISTS users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100)
);

INSERT INTO users (name, email) VALUES 
  ('สมชาย', 'somchai@example.com'),
  ('สมหญิง', 'somying@example.com'),
  ('Docker User', 'docker@example.com');
EOF

# สร้าง nginx/default.conf
$ mkdir nginx
$ cat > nginx/default.conf << 'EOF'
server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    
    location /api/ {
        proxy_pass http://api:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}
EOF

# สร้าง docker-compose.yml
$ cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  # Nginx - Web Server & Reverse Proxy
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api

  # Node.js API
  api:
    build: ./api
    environment:
      - DB_HOST=db
      - DB_USER=admin
      - DB_PASSWORD=secret
      - DB_NAME=myapp
    depends_on:
      db:
        condition: service_healthy

  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: myapp
    volumes:
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d myapp"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
EOF

# โครงสร้าง project
$ tree
.
├── api/
│   ├── Dockerfile
│   ├── package.json
│   └── server.js
├── database/
│   └── init.sql
├── docker-compose.yml
├── frontend/
│   └── index.html
└── nginx/
    └── default.conf

# Start all services
$ docker compose up -d

# Output:
# [+] Running 4/4
#  ✔ Network fullstack-demo_default  Created
#  ✔ Container fullstack-demo-db-1   Healthy
#  ✔ Container fullstack-demo-api-1  Started
#  ✔ Container fullstack-demo-nginx-1 Started

# ตรวจสอบ status
$ docker compose ps
NAME                        SERVICE   STATUS          PORTS
fullstack-demo-db-1         db        running (healthy)
fullstack-demo-api-1        api       running         
fullstack-demo-nginx-1      nginx     running         0.0.0.0:8080->80/tcp

# เปิด browser ไปที่ http://localhost:8080
# จะเห็นหน้าเว็บ + data จาก database!

# ดู logs
$ docker compose logs -f

# Cleanup
$ docker compose down -v
```

```
╔═══════════════════════════════════════════════════════════════════════╗
║              Docker Compose Architecture                              ║
╠═══════════════════════════════════════════════════════════════════════╣
║                                                                       ║
║  Browser                                                              ║
║    │                                                                  ║
║    │ http://localhost:8080                                            ║
║    ▼                                                                  ║
║  ┌────────────────────────────────────────────────────────────────┐   ║
║  │  Docker Compose Network (fullstack-demo_default)               │   ║
║  │                                                                │   ║
║  │   ┌─────────────────────────────────────────────────────────┐  │   ║
║  │   │  nginx (Port 80)                                        │  │   ║
║  │   │  ┌─────────────────┐  ┌─────────────────┐               │  │   ║
║  │   │  │  Static files   │  │  Reverse proxy  │               │  │   ║
║  │   │  │  /frontend/*    │  │  /api/* → api   │               │  │   ║
║  │   │  └─────────────────┘  └────────┬────────┘               │  │   ║
║  │   └─────────────────────────────────┼───────────────────────┘  │   ║
║  │                                     │                          │   ║
║  │                                     ▼                          │   ║
║  │   ┌─────────────────────────────────────────────────────────┐  │   ║
║  │   │  api (Port 3000)                                        │  │   ║
║  │   │  Node.js + Express                                      │  │   ║
║  │   │  GET /api/health                                        │  │   ║
║  │   │  GET /api/users                                         │  │   ║
║  │   └─────────────────────────────────┬───────────────────────┘  │   ║
║  │                                     │                          │   ║
║  │                                     ▼                          │   ║
║  │   ┌─────────────────────────────────────────────────────────┐  │   ║
║  │   │  db (Port 5432)                                         │  │   ║
║  │   │  PostgreSQL                                             │  │   ║
║  │   │  Database: myapp                                        │  │   ║
║  │   │  Volume: pgdata                                         │  │   ║
║  │   └─────────────────────────────────────────────────────────┘  │   ║
║  │                                                                │   ║
║  └────────────────────────────────────────────────────────────────┘   ║
║                                                                       ║
╚═══════════════════════════════════════════════════════════════════════╝
```

---

## 10. Best Practices และข้อควรระวัง

### 10.1 Dockerfile Best Practices

```dockerfile
# ══════════════════════════════════════════════════════════════
# DOCKERFILE BEST PRACTICES
# ══════════════════════════════════════════════════════════════

# ✅ 1. ใช้ specific tag แทน latest
FROM node:20-alpine       # ดี
FROM node:latest          # ไม่ดี - version อาจเปลี่ยน

# ✅ 2. ใช้ Alpine images (เล็กกว่ามาก)
FROM node:20-alpine       # ~140MB
FROM node:20              # ~1.1GB

# ✅ 3. Order instructions ตาม frequency of change
#    (จากเปลี่ยนน้อย → เปลี่ยนบ่อย เพื่อ cache efficiency)
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./     # เปลี่ยนน้อย
RUN npm ci                # cache ได้
COPY . .                  # เปลี่ยนบ่อย - ต้องอยู่ท้าย

# ✅ 4. ใช้ npm ci แทน npm install สำหรับ CI/CD
RUN npm ci --only=production

# ✅ 5. ใช้ non-root user
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nextjs -u 1001 -G nodejs
USER nextjs

# ✅ 6. เพิ่ม health check
HEALTHCHECK --interval=30s --timeout=3s \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# ✅ 7. ใช้ multi-stage builds สำหรับ production
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
CMD ["node", "dist/server.js"]
```

### 10.2 Docker Compose Best Practices

```yaml
# ══════════════════════════════════════════════════════════════
# DOCKER COMPOSE BEST PRACTICES
# ══════════════════════════════════════════════════════════════

version: '3.8'

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    # ✅ 1. ใช้ restart policy
    restart: unless-stopped
    
    # ✅ 2. กำหนด resource limits
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    
    # ✅ 3. ใช้ env_file แทน environment
    env_file:
      - .env
    
    # ✅ 4. ใช้ depends_on กับ health check
    depends_on:
      db:
        condition: service_healthy
    
    # ✅ 5. กำหนด network อย่างชัดเจน
    networks:
      - backend
      - frontend

  db:
    image: postgres:16-alpine
    # ✅ 6. กำหนด volume สำหรับ data persistence
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    
    # ✅ 7. Health check สำหรับ database
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  backend:
  frontend:

volumes:
  pgdata:
```

### 10.3 Security Best Practices

```
╔════════════════════════════════════════════════════════════════════════╗
║                    SECURITY BEST PRACTICES                             ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  1. ❌ อย่าใส่ secrets ใน Dockerfile หรือ docker-compose.yml              ║
║     # ไม่ดี:                                                             ║
║     ENV DB_PASSWORD=mysecretpassword                                   ║
║                                                                        ║
║     # ดี: ใช้ .env file หรือ Docker secrets                               ║
║     env_file:                                                          ║
║       - .env.local  # อย่า commit file นี้                                ║
║                                                                        ║
║  2. ✅ ใช้ non-root user                                                ║
║     USER node                                                          ║
║                                                                        ║
║  3. ✅ Scan images for vulnerabilities                                 ║
║     docker scout cves my-image:latest                                  ║
║                                                                        ║
║  4. ✅ ใช้ read-only volumes เมื่อเป็นไปได้                                 ║
║     volumes:                                                           ║
║       - ./config:/app/config:ro                                        ║
║                                                                        ║
║  5. ✅ จำกัด network access                                             ║
║     # Database ไม่ควร expose port ออก host                              ║
║     # ใช้ internal network เท่านั้น                                        ║
║                                                                        ║
║  6. ✅ เพิ่มใน .gitignore                                                ║
║     .env                                                               ║
║     .env.local                                                         ║
║     *.pem                                                              ║
║     *.key                                                              ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 10.4 Common Mistakes to Avoid

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    COMMON MISTAKES TO AVOID                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ❌ Mistake 1: ไม่ใช้ .dockerignore                                       │
│     → node_modules ถูก copy เข้า image (ช้ามาก!)                           │
│     ✅ สร้าง .dockerignore:                                              │
│        node_modules                                                     │
│        .git                                                             │
│        *.log                                                            │
│                                                                         │
│  ❌ Mistake 2: ใช้ latest tag                                            │
│     → version อาจเปลี่ยนกะทันหัน                                            │
│     ✅ ระบุ version ชัดเจน: node:20.10-alpine                             │
│                                                                         │
│  ❌ Mistake 3: ลืม depends_on                                            │
│     → API start ก่อน database พร้อม                                       │
│     ✅ ใช้ depends_on + healthcheck                                      │
│                                                                         │
│  ❌ Mistake 4: Hardcode passwords ใน docker-compose.yml                 │
│     → Secrets ติด Git history                                            │
│     ✅ ใช้ .env file + .gitignore                                        │
│                                                                         │
│  ❌ Mistake 5: ไม่ใช้ volumes สำหรับ database                              │
│     → Data หายเมื่อ container ถูกลบ                                        │
│     ✅ volumes: - pgdata:/var/lib/postgresql/data                       │
│                                                                         │
│  ❌ Mistake 6: Expose database port ออก host                            │
│     → Security risk!                                                    │
│     ✅ ให้ database อยู่ใน internal network เท่านั้น                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 11. เตรียมพร้อมสำหรับ Lab

### 11.1 สิ่งที่ต้องเตรียม

```
╔════════════════════════════════════════════════════════════════════════╗
║                    CHECKLIST ก่อนเริ่ม LAB                                ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  ✅ Docker ติดตั้งและทำงานได้                                              ║
║     $ docker --version                                                 ║
║     $ docker run hello-world                                           ║
║                                                                        ║
║  ✅ Docker Compose พร้อมใช้งาน                                           ║
║     $ docker compose version                                           ║
║                                                                        ║
║  ✅ มีบัญชี Docker Hub                                                    ║
║     $ docker login                                                     ║
║                                                                        ║
║  ✅ เข้าใจ concepts พื้นฐาน                                               ║
║     • Image vs Container                                               ║
║     • Dockerfile                                                       ║
║     • Docker Compose                                                   ║
║     • Network & Volumes                                                ║
║                                                                        ║
║  ✅ ทำตาม Demo exercises เรียบร้อย                                       ║
║     • รัน Nginx container                                               ║
║     • รัน PostgreSQL container                                          ║
║     • Build custom Node.js image                                       ║
║     • Push image ไป Docker Hub                                         ║
║     • รัน Docker Compose stack                                          ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

### 11.2 Lab Preview: N-Tier Architecture บน Docker

```
┌────────────────────────────────────────────────────────────────────────┐
│                    LAB: N-Tier Docker Deployment                       │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ใน Lab นักศึกษาจะ containerize N-Tier Task Board Application:           │
│                                                                        │
│  Components ที่ต้องสร้าง:                                                  │
│                                                                        │
│  1. API Container                                                      │
│     ┌───────────────────────────────────────────────────────────┐      │
│     │  • Dockerfile สำหรับ Node.js API                           │      │
│     │  • Layered Architecture (Controllers/Services/Repos)      │      │
│     │  • Environment variables สำหรับ DB connection              │      │
│     │  • Health check endpoint                                  │      │
│     └───────────────────────────────────────────────────────────┘      │
│                                                                        │
│  2. Database Container                                                 │
│     ┌───────────────────────────────────────────────────────────┐      │
│     │  • PostgreSQL image                                       │      │
│     │  • Init script สำหรับสร้าง tables                           │      │
│     │  • Volume สำหรับ persistent data                           │      │
│     └───────────────────────────────────────────────────────────┘      │
│                                                                        │
│  3. Nginx Container                                                    │
│     ┌───────────────────────────────────────────────────────────┐      │
│     │  • Reverse proxy ไปยัง API                                 │      │
│     │  • Serve static frontend files                            │      │
│     │  • SSL/HTTPS configuration                                │      │
│     └───────────────────────────────────────────────────────────┘      │
│                                                                        │
│  4. Docker Compose                                                     │
│     ┌───────────────────────────────────────────────────────────┐      │
│     │  • Orchestrate ทุก containers                              │      │
│     │  • Network configuration                                  │      │
│     │  • Volume management                                      │      │
│     │  • Health checks & dependencies                           │      │
│     └───────────────────────────────────────────────────────────┘      │
│                                                                        │
│  5. Analysis Assignment                                                │
│     ┌───────────────────────────────────────────────────────────┐      │
│     │  • เปรียบเทียบ VM vs Docker deployment                      │      │
│     │  • วิเคราะห์ข้อดีข้อเสีย                                        │      │
│     │  • สรุปการเรียนรู้                                            │      │
│     └───────────────────────────────────────────────────────────┘      │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 11.3 Quick Reference Card

```
╔════════════════════════════════════════════════════════════════════════╗
║                    DOCKER QUICK REFERENCE                              ║
╠════════════════════════════════════════════════════════════════════════╣
║                                                                        ║
║  IMAGES                        CONTAINERS                              ║
║  ─────────────────────────     ─────────────────────────               ║
║  docker pull <image>           docker run -d <image>                   ║
║  docker images                 docker ps                               ║
║  docker rmi <image>            docker ps -a                            ║
║  docker build -t name .        docker stop <container>                 ║
║  docker push <image>           docker start <container>                ║
║                                docker rm <container>                   ║
║                                docker logs <container>                 ║
║                                docker exec -it <c> sh                  ║
║                                                                        ║
║  DOCKER COMPOSE                                                        ║
║  ─────────────────────────────────────────────────────                 ║
║  docker compose up -d          Start all services                      ║
║  docker compose down           Stop and remove                         ║
║  docker compose down -v        Stop, remove + volumes                  ║
║  docker compose ps             List services                           ║
║  docker compose logs -f        Follow logs                             ║
║  docker compose build          Build images                            ║
║  docker compose exec api sh    Shell into service                      ║
║                                                                        ║
║  PORT MAPPING                  VOLUME MOUNTING                         ║
║  ─────────────────────────     ─────────────────────────               ║
║  -p 8080:80                    -v name:/path (named)                   ║
║  -p 3000:3000                  -v ./src:/app (bind)                    ║
║  (host:container)              (host:container)                        ║
║                                                                        ║
║  CLEANUP                                                               ║
║  ─────────────────────────────────────────────────────                 ║
║  docker system prune           Remove unused data                      ║
║  docker volume prune           Remove unused volumes                   ║
║  docker image prune            Remove unused images                    ║
║                                                                        ║
╚════════════════════════════════════════════════════════════════════════╝
```

---

## 📚 สรุป

### สิ่งที่เรียนรู้ในบทนี้

1. **ทำไมต้อง Docker** - แก้ปัญหา VM deployment (setup ยาก, environment ต่างกัน, cleanup ยุ่งยาก)

2. **Docker Concepts** - Image, Container, Dockerfile, Network, Volume, Registry

3. **Docker Hub** - Public registry สำหรับ share images

4. **Docker Commands** - pull, run, build, push, exec, logs

5. **Dockerfile** - Script สร้าง custom images

6. **Docker Compose** - จัดการ multi-container applications

7. **Best Practices** - Security, performance, reliability

### เตรียมพร้อมสำหรับ

- **Lab Week 6 V2**: N-Tier Architecture บน Docker
- **Week 7**: Cloud & Containerization (Kubernetes overview)
- **Term Project**: Deploy ด้วย Docker Compose

---

## 🔗 Resources เพิ่มเติม

- [Docker Documentation](https://docs.docker.com/)
- [Docker Hub](https://hub.docker.com/)
- [Play with Docker](https://labs.play-with-docker.com/) - ทดลองใช้ Docker ออนไลน์
- [Docker Curriculum](https://docker-curriculum.com/)
- [Dockerfile Best Practices](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)

---

**🎯 Next: Lab - N-Tier Architecture บน Docker (Version 2)**
